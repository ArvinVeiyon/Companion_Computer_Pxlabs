# Lets the AutoNav registration watchdog re-register the external mode after an
# FC reboot without a password. ONE command, nothing else. 2026-09-19.
roz ALL=(root) NOPASSWD: /usr/bin/systemctl restart rover-autonav-mode
