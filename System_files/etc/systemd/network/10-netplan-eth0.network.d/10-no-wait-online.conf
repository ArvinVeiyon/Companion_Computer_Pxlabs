# netplan's `optional: true` (60-eth0-recovery.yaml) does not emit this key on
# this netplan version; without it an unplugged cable can hang boot for ~2 min
# if systemd-networkd-wait-online is ever re-enabled (it is disabled today).
[Link]
RequiredForOnline=no
