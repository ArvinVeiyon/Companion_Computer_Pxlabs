# Vind-Roz Platform Memory
> Auto-loaded each session; also the Phi-3 offline prompt. Live: `~/.claude/projects/-home-roz/memory/` | Backup: `~/codex-work/memory/` → ArvinVeiyon/Companion_Computer_Pxlabs
> ⚠️ **BACKUP IS MANUAL:** `cp -p .../memory/*.md ~/codex-work/memory/` + push. **Never `rsync --delete`** — the mirror is a UNION of two scopes; the 2nd (`-home-roz-codex-work`) is NOT auto-loaded.
> ⚠️ **KEEP UNDER 125 LINES / 17 KB.** One line per entry. **Technical detail belongs in the two manuals below — put a POINTER here, not the fact.**

## 📘 THE TWO MANUALS — READ THESE BEFORE ASKING THE OPERATOR OR DERIVING ANYTHING
📐 **`~/ros2_ws/docs/autonav_reference.md`** — *what is TRUE and why.* **§5 constants WITH THEIR METHODS · §10 known faults · §11 inviolable rules · §12 test ladder · §13 DATED FITNESS.**
📕 **`~/ros2_ws/docs/setup_manual.md`** — *what to DO.* **§C NODE INVENTORY (real ExecStart+params; §C10: an external mode's requirements come from the SETPOINT TYPE) · §D6 arming AutoNav · §E2b map verification · §A7 param changelog (`RO_*` rover-only vs `EKF2_*` SHARED WITH THE DRONE)**. ⚠️ Part A mostly UNVERIFIED.
🔑 **Read §5 before trusting any constant · §11 before designing behaviour · setup_manual C before touching a node · E before running a procedure.**
📗 **`docs/README.md` = THE DOC INDEX — open it, don't duplicate it here.** Traps: **`autonomy_plan.md` M0-M4 = the ONLY mode numbering** · ⚠️ `ros2_architecture.md` STALE · 🗄 `roadmap.md` archived. 🔑 **08-10: the doc that disagreed with three others was the accurate one — count of sources ≠ weight of evidence.**
🔑 **Arming AutoNav: external mode, NO RC switch slot** ⇒ RC arm always lands in **Manual**. Path = arm in Manual (throttle neutral) → companion sends `DO_SET_MODE main=4 sub=11`. ⛔ **never arm AutoNav on stands with the EKF bridge running** (self-sustaining limit cycle; only a disarm stops it). → setup_manual §D6

## [MEMORY_FILES]
**feedback (RULES) — 🔴 OPEN THE FILE, THE ONE-LINER IS NOT THE RULE:** `debug_before_documenting` **(DEBUG FIRST; docs only for what changes a decision)** · `scope_px4_params_by_control_flags` **(scoped by `flag_control_*_enabled`, NOT the doc string — put the rover into a wall)** · `verify_after_editing` **(grep TWICE: the OLD CLAIM *and* what should have SURVIVED — this caught a near-loss on 08-22)** · `test_before_concluding` · `independent_rulers` · `check_docs_before_measuring` · `eliminate_hypothesis_whole_family` · `crash_recovery_checkpoint` · `use_dds_not_mavlink` · `mavlink_shell_session_exhaustion` · `camera_qgc_only` · `dkms_arch` · `wlan0_persistent_name`
**reference:** `px4_vio_collision` (🔴 **collision-prevention is MC-ONLY, dead on a rover; no PX4 path planner**) · `this_machine` · `wfb_ng` · `wfb_rlyctl` · `wfb_cfg_apply` · `uart_map` · `services` · `known_fixes_archive` · `gcs_companion_interface` · `todos.md` · `ros2_nodes`/`ros2_topics` · `rover_odometry`
**project — ACTIVE:** `fc_hardfaults` (🔴 the FC investigation, READ IT) · `indoor_mapping_slam` (§13-16 = the 08-09 story) · `rover_autonav` (the wall-calibration record) · `perception_3d_costmap` · `autonomy_plan_reframe`
**project — other:** `rc_ch10_reboots_companion` · `l4_gemini_nav2_prereqs` · `vision_multicam_upgrade` · `l2_floortest_wheel0_reversed` · `wfb_undervoltage_dead_nic` · `external_wifi_uplink` · `gcs_link_degraded` · `relay_ntp_setup` · `relay2_relaystn` · `companion_network_degraded` · `boxb_pcie_usb` · `codexrelay_divergence` · `codexwork_token_in_remote` (🔴 PAT) · `codexwork_branches`

## [WORKING ON THIS MACHINE] — things the manuals do NOT cover
⚠️ **sudo NEEDS the password:** `printf '1987\n' | sudo -S …`
⚠️ **`pkill -f <pat>` MATCHES MY OWN SHELL** (⇒ self-kill, exit 144). Use `pgrep -x` / `pgrep -f "[p]attern"` then `kill <pid>`.
🔴 **I AM A MAJOR CPU CONSUMER — BUDGET FOR IT:** `claude` runs **55-86% of a core**. Power check = `vcgencmd get_throttled` (0x0 clean).
✅ **I can read/write PX4 params myself:** `python3 ~/ros2_ws/tools/set_param.py NAME [value]` (RAM only, finish with `param save`; refuses while armed). **Don't ask the operator to read QGC.**
📄 **Measurement hygiene · `cpu_catcher.sh` · ghost `node list` · "prove the DETECTOR was live too" → `this_machine`.** ⚠️ **Boot clock WRONG until NTP steps it.** ⚠️ **Replays = `ROS_DOMAIN_ID=42`, live = 0.**

## [CURRENT STATE — 2026-08-13 late. ARMED SUCCESSFULLY 08-10 (S1) — the old "NEVER ARMED" is WITHDRAWN]
✅ **08-21 CAN IS BACK (operator returned it) — MEASURED:** `esc_status` live, `esc_count 4`, **flags 15**, 24.67 V, errorcount 0 ⇒ **the 08-20 "CAN GONE / rover IMMOBILE" state is WITHDRAWN; todos (a)(d)(f)(h) UNBLOCKED.** ⚠️ **flags=15 is BUS PRESENCE, not motion — re-verify `/odom` under actual wheel motion.**
🔑 **THE 08-20 CAN ELIMINATION STILL STANDS** (those 3 faults had **NO BUS PRESENT**) ⇒ CAN-LOAD FAMILY PERMANENTLY DEAD AS A TRIGGER. ⛔ **Don't re-suspect CAN; don't blame the reconnection for any NEW fault.** ⚠️ **the clean no-bus TEST CONDITION is GONE** — runs from 08-21 carry CAN load; say so when comparing fault counts across that boundary. 🔑 **`wq:uavcan` went quiet 08-20 only because it was IDLE — the victim tracks WHICH TASK IS BUSY; a shift in victim is NOT progress.**
🛑 **STOP CALIBRATING `erpm_to_ms` (operator, 08-13) — proved right, but the REASON given ("no single constant can be right") was WRONG: one IS right. The defect was never the scale.** ⇒ **gate safety on `/scan` CLEARANCE, not `/odom`.**
⚠️ **`ros2_ws` has UNCOMMITTED doc/tool edits** on **`fix/collision-perception-health-gate` (NOT main)** — incl. `fc_hardfault_analysis.md` + `ftp_pull_faults.py` (08-20). ⚠️ **`nav_state` may still read 23 (AutoNav, inert) — clear it on the RC first.**
🔑 **THERE IS NO VIO, AND NEVER WAS** — position = wheel ERPM + camera gyro dead reckoning; **RTAB-Map CONSUMES our TF odom** and adds `map→odom`, like AMCL. **Don't propose adding VIO.** → reference §6
🔑 **PX4 NEVER KNOWS WHERE IT IS, BY DESIGN** — the bridge sends VELOCITY only; **position lives in ROS, not the FC.** ⚠️ **always state the bridge's state when quoting `v_xy_valid`** (false with it stopped, true with it running).
🔴 **08-16 LOCALIZATION IS DEAD — 1213 REJECTED, *ZERO* ACCEPTED** (same map/config worked 08-09). 🔑 **TRAP: `map→odom` CHANGING is NOT health — with 0 fixes accepted that is ODOM DRIFT. CONFIRM VIA THE ACCEPTED-FIX COUNT.** ⏭ **live `camera_info` vs the calibration in `house_map_v4.db`. ⛔ DON'T MAP THE HOUSE FIRST.** → `indoor_mapping_slam`
✅ **2D GRID IS FINE (08-16, `tools/grid_review.py`)** — ⛔ **Do NOT "fix" it:** `MaxGroundHeight` 0.28 = 3× truth, **keep 0.10**; `NormalsSegmentation false` = the spikes that got v5 rejected. Plate contamination 0.06% (11.8% was CLOUD-only).
🔴 **THE REAL CONSTRAINT IS THE ROOM: ~2 m² open floor vs a 0.73×0.56 m rover.** ⛔ **T2 CANNOT RUN HERE** — needs ~3.05 m clearance, aborts before moving. ⏭ operator: corridor (recommended) · re-scope · or drop this room as the M3 target.
✅ **"uXRCE UART REBOOTS THE COMPANION" = RC CH10, NOT A FAULT** — `rc_control_node` reads CH10 off `/fmu/out/input_rc`: **2014=reboot · 1514(MIDDLE)=shutdown · 1011=down=safe.** 🔑 **CHECK CH10 BEFORE debugging any companion restart or rebooting the FC.** → `rc_ch10_reboots_companion`
🔴 **08-16 CAMERA PHYSICALLY ROTATED ⇒ EVERY CAMERA-REFERENCED CALIBRATION IS SUSPECT:** `front_overhang` · `/scan` scale · standoff · gyro-heading TF (locked at `rover-odometry` START) · localization vs `house_map_v4`. **The 1.54° roll figure is now meaningless.** ⏭ `wall_probe.py` → fix roll in TF → restart `rover-odometry` → re-tape, BEFORE any moving test.
  🔴🔴 **DOC TRAP (audited 08-21): `autonav_reference.md` STOPS AT 08-14 AND STILL SHOWS 0.345 / 0.337 / 0.9845 / 1.54° AS ✅ "TAPE-CONFIRMED".** The rotation is recorded in **NO doc at all — only here.** ⛔ **§5 is the doc this index tells you to trust for constants; while the roll is unfixed it is WRONG-BY-OMISSION. Do not read a ✅ in §5 as current.**
⚠️ **`autonomy_plan.md` HAS THE L0–L5 LADDER TWICE** (~L278, ~L429) — flagged in-file, unresolved, don't plan off either. **M-numbers are the ONLY mode numbering** (a 3rd scheme got `roadmap.md` archived).
🗺️ **GOOD ROOM MAP: `~/house_map_v4.db`** (bag `~/map_run_20260809_185011`, 0 drops) — shape/objects/floor correct, **walls SINGLE**, scale tape-verified. `rtabmap_localization.yaml` points at it.
✅ **08-16 retest protocol — reuse it.** → autonav_reference §14
✅ **ODOMETRY (unblocked 08-21): `erpm_to_ms`=0.003900 CLOSED, tape-validated — NEVER RE-OPEN THE SCALE.** 🔑 the defect is the **ESC ZERO-DROPOUT** (~24% of the 23.3% crawl under-read). ⚠️ a hand spin measures SLIP-FREE (units trap). 🔴 **`si_motor_poles` ↔ `erpm_to_ms` = LINKED PAIR — "fixing" poles in VESC Tool SILENTLY HALVES `/odom`.** 🔑 **ADDR 10=RF(INVERTED) · 11=FL · 13=RL · 12=RR.** ⛔ **RECORD `esc_status` IN BAGS.** → autonav_reference §5/§10/§13
🔬 **DISPROVEN 08-09, don't re-propose:** map coverage · light/auto-exposure (⚠️ tested in DAYLIGHT — "dark room hurts matching" still open) · rover inside the 0.308 m depth min · camera-gyro rate-dependent scale (was my probe's arrival-time integration) · `yaw_source: wheels` (skid-steer scrub).
🔴 **FC HEADING IS UNUSABLE — 2 UNTESTED SUSPECTS.** It is the **EKF's FUSED YAW, not the gyro**: **`bmm350`** (`EKF2_MAG_TYPE=1`, the only indoor yaw observer) · **GPS** (`EKF2_GPS_CTRL=7`). Test separately. **Rover-only — the DRONE shares this FC and needs both.** → autonav_reference §6
⏭ **QUEUED:** bridge `vehicle_angular_velocity` to DDS (commented out at `dds_topics.yaml:60`; 3 fused IMUs @398 Hz, would **decouple odometry from the camera**) — needs a FC flash, pair it with other FC work.

✅ **S1 kill-switch PASSED** 07-22, re-confirmed 08-10 (`tools/s1_kill_test.py`). → autonav_reference §12/§13.
🔑🔑 **EVERY AUTONAV RUN NEEDS `rover-ekf-bridge` STARTED FIRST** (FLOOR only — limit-cycle hazard is wheels-UP; **stop it after**). Indoors it is the ONLY source of the local VELOCITY estimate AutoNav requires. Signature without it: **mode refused ARMED but accepted DISARMED**. → `setup_manual.md` §C10

✅ **REFLEX COLLISION-STOP: fail-open FIXED 08-10, all 4 validations pass** — perception-health gate BEFORE the clearance test (`min_valid_fraction`=0.35); the threshold was never wrong. 🔑 **CUE: `BLOCK forward (BLIND)` + `scan_fresh=yes`.** → autonav_reference §8/§12
🔴🔴 **SPEED COMMANDS ARE NOT HONOURED — SAFETY-CRITICAL. 0.05→0.140 · 0.25→~0.9 m/s; floor ~0.14 = `RO_SPEED_TH` (0.10)**: firmware zeroes feedback below it ⇒ the loop re-accelerates — **structural.** ⛔ **RETRACTED "`RO_MAX_THR_SPEED` understated 7.4×" — closed-loop data CANNOT identify a plant gain; needs an OPEN-LOOP sweep.** ⛔ **GATE EVERY MOVING TEST ON MEASURED SPEED, NEVER THE COMMAND** (I hit the wall 08-12 sizing off the command; coast ~0.30 m); odom backstop useless at crawl — use `--min-bumper`. ⛔ T2 at crawl only. → autonav_reference §10/§13
✅ **STANDOFF 0.345 m (n=3, tape) — SPEED-BOUND by the COAST, not the threshold** (at ~0.9 m/s it left 0.020 m and CONTACTED). **`/scan` scale 0.9845, `front_overhang` 0.337**, reads SHORT ⇒ reflex fires EARLY (safe). ⚠️ **read autonav_reference §5/§12/§13 first — 2 traps.**
🔴🔴 **08-16 I KILLED THE GCS MAVLINK LINK — ONE `mavlink_shell.py` PER FC BOOT, NEVER RETRY AN EMPTY RESULT** (sessions leak; the FC stayed ALIVE, only its MAVLink instance died — DDS healthy, NO fault log). **Recovery = reboot over DDS.** → `mavlink_shell_session_exhaustion`
🔴 **TRAPS THAT WILL BITE AGAIN:** a **DISARMED external mode reads as SELECTED but is NOT RUNNING** (false-pass generator) · **never quote 'last scan → wheels stopped' as reflex latency** · **`/scan` does NOT come back after a scan-killing test and `is-active` LIES.** ⛔ **Never read a quiet topic as evidence — prove a NON-ZERO baseline first.** → autonav_reference §10

## [TODOS] → memory/todos.md
**⏭ NEXT → `todos.md`: (b) GATE SAFETY ON `/scan`, NOT `/odom`** · (c) FC hardfaults (below) · (e) localization COMMIT `map→odom`? (gates M3) · (g) S2b · #21 gyro-yaw. ✅ **UNBLOCKED 08-21 BY THE CAN RETURN: (a) ESC zero-dropout · (d) open-loop `RO_MAX_THR_SPEED` sweep · (h) `si_motor_poles`.** ⛔ **(f) T2 STAYS BLOCKED — by the ROOM (~2 m² floor vs 3.05 m needed), not by CAN.** ⛔ `erpm_to_ms` CLOSED.
⏭⏭ **08-22 PAUSED MID-EXPERIMENT, OPERATOR RETURNS LATER TODAY → `project_fc_hardfaults.md` "RESUME HERE" (top).** Factory param reset done ~10:22, `SYS_AUTOSTART=50000` restored + rebooted ~10:41; commander + DDS + 814 params back; **card CLEAN, last fault #36 at 10:22:31, zero reboots since.** 🔴🔴 **DON'T SCORE THE SOAK YET — `UAVCAN_ENABLE=0` ⇒ DroneCAN/ESC OFF, and `wq:uavcan` was 7/35 faults incl. 3 of the last 4 ⇒ NOT LOAD-COMPARABLE. Set `UAVCAN_ENABLE=3` + reboot FIRST.** ⛔ **`RC_MAP_KILL_SW=0` — KILL SWITCH UNMAPPED, no calibration: DO NOT ARM.** 🔴 **restore it to ch12, NOT the `2026-05-29.params` value 8 (that IS the known-bad ch12→ch8 move).** ✅ **OPERATOR HAS A PRE-RESET PARAM BACKUP AND WILL SHARE IT ⇒ the before/after diff is BACK ON — the highest-value analysis available.** ⏭ **diff it against `~/fc_param_backups/fc_params_20260822_DEFAULTS_frame50000.params` (814, factory defaults + frame 50000, captured 11:04); ignore `CAL_*`. What's left = the 2.1.0 contamination candidates.**
🔴🔴 **FC HARDFAULTS — 36 faults archived. STATE, PER-FAULT TABLE, METHOD, ALL CAVEATS → `project_fc_hardfaults.md` TOP + `docs/fc_hardfault_analysis.md` §17-§18. OPEN ONE; THE ONE-LINER IS NOT THE STATE.** 🔑 **ROOT CAUSE = NULL-POINTER DEREF / control-flow corruption** (7/35 faults ran `pc` OUTSIDE flash — 0x0, 0x10, 0x1b22…; cfsr CLASS was always mixed, so a run of one class is noise). 🔑 **QUIET PROVES NOTHING (89 min, then 6.5 h fault-free) — judge on COUNT over a fixed window.**
  🔑🔑 **THE VICTIM TASK IS A LOAD MAP, NOT A DEFECT MAP — CAN A/B PROVES IT** (`wq:uavcan` = 0 victims 08-20 bus-absent, 3 of 4 faults 08-22 bus-back) ⇒ **removing a peripheral RELOCATES the fault, never stops it — and a fault-rate drop after ANY change may just be lost LOAD.** ✅ **CAN, microXRCE and the RC CRSF UART have all now been physically removed and faults continued ⇒ MAVLink is the ONLY companion link never removed.**
  ✅ **08-22 IMU BOARD *AND* A 4th FC BOARD SWAPPED — FAULT #35 HIT ANYWAY ⇒ HARDWARE COMPREHENSIVELY ELIMINATED** (4 FC boards · 2 IMU boards · 2 SD cards · both power modules · CAN). ⚠️ ask if the IMU *cable* was changed too. ⛔ **DON'T RE-SUSPECT** any of those, plus: firmware version · the COMPANION (BASE-RATE TRAP) · stack exhaustion (min `sp−bottom` headroom = **1096 B**; `used==size` is a coloring artifact) · the ULog `ram_usage` test · the autonav node. ⛔ **NEVER SWAP THE FC.**
  🔴🔴 **ELF TRAP — ⛔ NEVER RESOLVE FAULT ADDRESSES AGAINST THE COMMITTED FIRMWARE.** `f0889f3d10`'s ELF matches the fault log's build datetime TO THE SECOND and is still the WRONG BINARY (running FW = **2.1.0, unrecorded dirty tree**, only *reports* that hash). It produced a confident, wrong `ucdr_serialize_esc_status` lead — **RETRACTED**. 🔑 **PROVE FIRMWARE IDENTITY FIRST: `comm -23` live `/fmu/out/*` vs the commit's `dds_topics.yaml`.** ⚠️⚠️ **`ftp_pull_faults.py` RUNS OVER MAVLINK FTP = upstream PX4 #22160's exact trigger — I AM INJECTING THE SUSPECT; prefer the DDS reboot detector, pull once at the END of a window.**
  🔴 **SUSPECTS LEFT: the 2.1.0 FIRMWARE IMAGE (unrecorded — cannot be rebuilt or analysed)** · **PARAM CONTAMINATION (08-15)** · MAVLink (untested) — ⛔ **DO NOT LOAD `PXlabs_..._tested_2026-05-29.params` wholesale: PRE-TUNING, ZEROES every rover gain, MOVES THE KILL SWITCH ch12→ch8. Selective only + OPERATOR SIGN-OFF (FC is SHARED WITH THE DRONE).**
🔧 **PX4 PARAMS → `docs/px4_param_audit.md` + `setup_manual` §A7. PULL FROM THERE.** 🔴 **08-14 my param change caused a HARD WALL HIT IN MANUAL — `RO_ACCEL_LIM`/`RO_DECEL_LIM` SLEW THE MANUAL STICK, so CENTRING IT NO LONGER STOPS THE ROVER. KEEP BOTH AT −1.** ⚠️ `set_param.py` writes **RAM ONLY** — finish with `param save`. 🔑 autopilot is `1:1`; **never read vehicle state from the GCS at `255:190`.**
**Numbered backlog → `todos.md`** (⚠️ **`5. Antenna tracker HW` is ONLY here — not in `todos.md`**). Traps only: 🔴 **NO onboard Wi-Fi fallback — recovery is WFB → relay:2222 ONLY** · ❌ **DELETED, don't re-propose (3+4+24):** GS `rx_ring_size`, GS TX power (maxed), trim PX4 MAVLink rates · ⏭ **27:** reflex `/scan_3d` is a PARAM (`collision.scan_topic`), still `/scan` — **needs ONE low/overhanging object `/scan` misses before flipping** · **#25 still keys `/dev/video0`**.
**[OUTDOOR] O1-O5:** STL-19 · DroneCAN GPS · lidar SLAM · GPS Nav2 · outdoor safety. **WFB parked: #22 = HARDWARE, reseat drone NIC-A ant0.**
⚠️ **`align_mode:=HW` untested** — would kill the depth-glitch class, save 71%/core. ⚠️ **Orbbec clone is GITIGNORED — a re-clone RESTORES #26** (patch `codex-work 16665f5`).

## [FPV CAMERAS] → `docs/vision_streaming.md` (autonomy camera is in setup_manual C2)
Both FPV-capable, port 6-2, **swap from QGC only**; **LG Smart Cam is current**. ⛔ **Never key a camera by `/dev/videoN` or by-id** — only `usbcam-<vidpid>-<serial>-i<iface>`; `rpivid`/`pispbe-*` renumber them. **⚡ FPV video costs `/scan` 28.4→22.3 Hz.** ⛔ **Do NOT modify the ffmpeg command line** (vetoed). ⚠️ **`vision_streaming` RUNNING ≠ streaming** (08-20: up, yet `/scan` 29.2 Hz).
## [WFB_NG] → `reference_wfb_ng.md` (**PARKED — only action left is HW: reseat drone NIC-A ant0**)
**Drone TX is flawless — when video breaks WFB's input queue is EMPTY: SUSPECT THE SOURCE.** ⚠️ **the measurement METHOD cost a week — read the file before measuring anything radio.**
## [RELAY_STATION] → `relay2_relaystn`
`vind-rly` RPi5 | `ssh vind-admin@10.5.5.77` (**sudo NEEDS A PASSWORD ⇒ journalctl of other units = "No entries"**) | tunnel 2222→drone :22 | **NO RTC**.
## [REPOS / GCS]
`codex-work`: `~/codex-work` → Companion_Computer_Pxlabs, branch **master** (origin/main stale) | `codex-relay`: on vind-rly → Relay_Station_Pxlabs | `ros2_ws`: `~/ros2_ws`, ⚠️ **on `fix/collision-perception-health-gate`, NOT main — check before assuming**
GCS: G-Control.exe → pxlabs_cli.exe → SSH relay:2222 → companion:22 | QGC: ArvinVeiyon/PXLABS_qgroundcontrol @ PXLABS-integration
🔴🔴 **2 LIVE PATs STILL UNREVOKED (08-20 sweep): `~/git_key` (fine-grained) + a CLASSIC scope-`repo` token in `~/.claude/history.jsonl`.** ⛔ **OPERATOR MUST REVOKE IN A BROWSER — no API can do it.** ✅ Revoking breaks nothing (all remotes SSH). 🔑 **NEVER PASTE A SECRET INTO A PROMPT — cleartext in `history.jsonl`.** **URLs, the 3rd token, `codex-relay` CLOSED → `codexwork_token_in_remote`**
## [SERVICE / TROUBLESHOOTING QUICK INDEX] → full detail in setup_manual C, D2, E5
**AIDE timer DISABLED** (~3.5 h/day of a core). **`tfmini` DISABLED — ⚠️ MUST `enable --now tfmini` for the DRONE.** **`rover-ekf-bridge` DISABLED on purpose** (wheels-up limit-cycle hazard). ✅ **08-22: all 6 other units ENABLED + running** (5 rover + `microxrce-agent`, plus `vision_streaming`). 🔴🔴 **THE FC WAS FACTORY-PARAM-RESET AT 10:22 — THE OLD HEALTH SNAPSHOT IS VOID.** Now: commander + DDS up, `SYS_AUTOSTART=50000`, but **`UAVCAN_ENABLE=0` ⇒ NO `esc_status`, NO ESC `/odom`**, no calibration, **kill switch unmapped**. (Pre-reset: `/scan` 30.1 Hz · `/odom` 99.8 Hz · ESCs 4/flags 15 — that CONFIRMED the CAN return end-to-end; expect it back once `UAVCAN_ENABLE=3`.) 🔑 **`ros2 node list` SHOWS GHOST NODES FOR MINUTES AFTER A STOP — prove a node dead with `pgrep`.** ⚠️ **the 08-22 restart RE-LOCKED the gyro-heading TF with the camera roll STILL UNCORRECTED** — the roll fix needs another `rover-odometry` restart.
no_MAVLink → `ttyAMA0` + the PX4 MAVLink instance; 🔑 **GCS lost MAVLink but DDS FINE ⇒ FC ALIVE, only its MAVLink instance died — reboot over DDS** | no_DDS → `microxrce-agent`, `ttyAMA4` | WFB_down → `wifibroadcast@drone` | **no_video → CPU-starvation latch FIRST** | no_scan/no_cloud → **half-dead camera restart** | localization degrading → **colour rate, restart camera**
