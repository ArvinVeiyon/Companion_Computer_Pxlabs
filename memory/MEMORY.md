# Vind-Roz Platform Memory
> Auto-loaded each session; also the Phi-3 offline system prompt. Live: `~/.claude/projects/-home-roz/memory/` | Backup: `~/codex-work/memory/` → GitHub ArvinVeiyon/Companion_Computer_Pxlabs
> ⚠️ **BACKUP IS MANUAL:** `cp -p .../memory/*.md ~/codex-work/memory/` + git push. **Never `rsync --delete`** — the mirror is a UNION of two scopes; the 2nd (`-home-roz-codex-work`) is NOT auto-loaded, check it after any codex-work session.
> ⚠️ **KEEP UNDER 125 LINES / 17 KB.** One line per entry. **Technical detail belongs in the two manuals below — put a POINTER here, not the fact.**

## 📘 THE TWO MANUALS — READ THESE BEFORE ASKING THE OPERATOR OR DERIVING ANYTHING
📐 **`~/ros2_ws/docs/autonav_reference.md`** — *what is TRUE and why*, 14 cited sections (its own TOC lists them). 🔑 **§5 constants WITH THEIR METHODS · §10 known faults · §11 inviolable rules · §12 test ladder · §13 DATED FITNESS ASSESSMENT.**
📕 **`~/ros2_ws/docs/setup_manual.md`** — *what to DO.* **§C NODE INVENTORY (real ExecStart+params; §C10 external modes: requirements come from the SETPOINT TYPE, not the mode) · §D6 arming into AutoNav · §E2b manual map verification · §A7 PX4 param changelog (`RO_*` rover-only vs `EKF2_*` SHARED WITH THE DRONE)**. ⚠️ Part A mostly UNVERIFIED-ON-THIS-VEHICLE (📋).
🔑 **Read §5 before trusting any constant · §11 before designing any behaviour · setup_manual C before touching a node · E before running a procedure.**
📗 **`docs/README.md` = THE DOC INDEX — open it, don't duplicate it here.** Only the traps: **`autonomy_plan.md` modes M0-M4 = the ONLY mode numbering** · ⚠️ `ros2_architecture.md` STALE (setup_manual C supersedes) · 🗄 `roadmap.md` archived (L-number clash = hazard).
✅ **08-10 safety conflict resolved.** 🔑 **the doc that disagreed with three others was the accurate one — count of sources ≠ weight of evidence.**
🔑 **Arming AutoNav: it is an external mode with NO RC switch slot** ⇒ RC arm always lands in **Manual**. Path = arm in Manual (throttle neutral) → companion sends `DO_SET_MODE main=4 sub=11`. ⛔ **never arm AutoNav on stands with the EKF bridge running** (self-sustaining limit cycle; only a disarm stops it). → setup_manual §D6

## [MEMORY_FILES]
**feedback (RULES) — 🔴 OPEN THE FILE, THE ONE-LINER IS NOT THE RULE:** `scope_px4_params_by_control_flags` **a `RO_*` param is scoped by `flag_control_*_enabled`, NOT its doc string — put the rover into a wall** · `verify_after_editing` **grep TWICE: the OLD CLAIM *and* what should have SURVIVED** · `test_before_concluding` · `independent_rulers` · `check_docs_before_measuring` · `eliminate_hypothesis_whole_family` · `crash_recovery_checkpoint` · `use_dds_not_mavlink` · `mavlink_shell_session_exhaustion` · `camera_qgc_only` · `dkms_arch` · `wlan0_persistent_name`
**reference:** `px4_vio_collision` (🔴 **PX4 collision-prevention is MC-ONLY, dead on a rover; no PX4 path planner**) · `wfb_ng` · `wfb_rlyctl` · `wfb_cfg_apply` · `uart_map` · `services` · `known_fixes_archive` · `gcs_companion_interface` · `todos.md` · `ros2_nodes`/`ros2_topics` · `rover_odometry`
**project — ACTIVE:** `indoor_mapping_slam` (§13-16 = the 08-09 story) · `rover_autonav` (the wall-calibration record) · `perception_3d_costmap` · `autonomy_plan_reframe`
**project — other:** `rc_ch10_reboots_companion` · `l4_gemini_nav2_prereqs` · `vision_multicam_upgrade` · `l2_floortest_wheel0_reversed` · `wfb_undervoltage_dead_nic` · `external_wifi_uplink` · `gcs_link_degraded` · `relay_ntp_setup` · `relay2_relaystn` · `companion_network_degraded` · `boxb_pcie_usb` · `codexrelay_divergence` · `codexwork_token_in_remote` (🔴 plaintext PAT) · `codexwork_branches`

## [WORKING ON THIS MACHINE] — things the manuals do NOT cover
⚠️ **sudo NEEDS the password:** `printf '1987\n' | sudo -S …`
⚠️ **Boot clock is WRONG until NTP steps it** — don't correlate journals across a boot.
⚠️ **`pkill -f <pat>` MATCHES MY OWN SHELL** (the pattern is in my command line ⇒ self-kill, exit 144). Use `pgrep -x` / `pgrep -f "[p]attern"` then `kill <pid>`.
🔴🔴 **I AM A MAJOR CPU CONSUMER — BUDGET FOR IT.** `claude` runs **55-86% of a core**; its npm update check and the pemmican MOTD hook are **DISABLED**. Power check = `vcgencmd get_throttled`, 0x0 clean.
🔑 **START A PERCEPTION MEASUREMENT, THEN GO QUIET** — commanding throughout starved the camera on 08-09 and gave two wrong answers. Spikes vanish before `ps`: **`tools/cpu_catcher.sh`**.
⚠️ **No rate/CPU number is trustworthy without `ps -eo pid,pcpu --sort=-pcpu` first.**
⚠️ **Replays need `ROS_DOMAIN_ID=42`; the LIVE stack is domain 0.** ⚠️ **`fps` is INERT in QGC.**
✅ **I can read/write PX4 params myself:** `python3 ~/ros2_ws/tools/set_param.py NAME [value]` (refuses while armed). **Don't ask the operator to read QGC.**

## [CURRENT STATE — 2026-08-13 late. ARMED SUCCESSFULLY 08-10 (S1) — the old "NEVER ARMED" is WITHDRAWN]
🛑 **08-13 STRATEGIC CALL — STOP CALIBRATING `erpm_to_ms`** (operator raised it). ✅✅ **PROVED CORRECT 08-13** — but ⚠️ **the reason given ("no single constant can be right") was WRONG; one IS right (0.003900). The defect was never the scale.** ⇒ **Gate safety on `/scan` CLEARANCE, not `/odom`.**
⚠️ **08-14: `ros2_ws` has UNCOMMITTED doc/tool edits** on branch **`fix/collision-perception-health-gate` (NOT main)**; last commit b4d945f · `codex-work` db19b74. ⚠️ **`nav_state` may still read 23 (AutoNav, inert) after a session — clear it on the RC first.**
🔑🔑 **THERE IS NO VIO, AND NEVER WAS** — position = wheel ERPM + camera gyro dead reckoning ONLY; **RTAB-Map CONSUMES our TF odom** (`odom_frame_id: odom`) and adds `map→odom`, like AMCL. ⇒ M1/M2 need no VIO/localization. **Don't propose adding VIO.** → reference §6
🔑 **PX4 NEVER KNOWS WHERE IT IS, BY DESIGN** — the bridge sends VELOCITY only. **Position lives in ROS, not the FC.** ⚠️ The 08-10 `v_xy_valid: false` reading was taken with the **bridge STOPPED**; with it running, 08-13 measured **true**. Always state the bridge's state when quoting those flags.
🔴🔴 **08-16 LOCALIZATION IS DEAD — 1213 REJECTED, *ZERO* ACCEPTED.** 🔑 REGRESSION: same map/config gave a usable pose on 08-09. 🔑🔑 **TRAP: `map→odom` CHANGING is NOT health — with 0 fixes accepted that is ODOM DRIFT. CONFIRM VIA THE ACCEPTED-FIX COUNT.** ⏭ **NEXT: live `camera_info` vs the calibration in `house_map_v4.db`. ⛔ DON'T MAP THE HOUSE FIRST.** → `indoor_mapping_slam`
✅ **2D GRID IS FINE (08-16, `tools/grid_review.py`)** — v4 gives 3.11 m² free vs ~2 m² real floor. ⛔ **Do NOT "fix" it:** `MaxGroundHeight` 0.28 → 3× the truth; `NormalsSegmentation false` → the spikes that got v5 rejected. **Keep 0.10.** Grid plate contamination 0.06% (11.8% was CLOUD-only). → `todos.md`
🔴🔴 **THE REAL CONSTRAINT IS THE ROOM: ~2 m² open floor vs a 0.73×0.56 m rover (0.41 m²).** ⛔ **T2 CANNOT RUN HERE** — it needs a clear 2 m corridor (~3.05 m clearance) and aborts before moving. **T2 is blocked by the ROOM, not just the FC.** ⏭ operator decision: run T2 in a corridor (recommended) · re-scope shorter · or rethink this room as the M3 target.
✅ **"uXRCE UART REBOOTS THE COMPANION" = RC CH10, NOT A FAULT.** `rc_control_node` reads CH10 off `/fmu/out/input_rc` ⇒ off its **down** detent latches `reboot` (2014) / `shutdown` (1514, MIDDLE). **1011 = down = safe.** 🔑 **CHECK CH10 BEFORE debugging any companion restart, and before rebooting the FC.** → `rc_ch10_reboots_companion`
🔴 **08-16 CAMERA PHYSICALLY ROTATED ⇒ EVERY CAMERA-REFERENCED CALIBRATION IS SUSPECT:** `front_overhang` 0.337 · `/scan` 0.9845 · 0.345 m standoff · gyro-heading TF (locked at `rover-odometry` START) · localization vs `house_map_v4`. **1.54° roll now meaningless.** ⏭ `tools/wall_probe.py` → fix roll in TF → restart `rover-odometry` → re-tape, BEFORE any moving test. → `todos.md`
⚠️ **`autonomy_plan.md` HAS THE L0–L5 LADDER TWICE** (~L278, ~L429) — flagged in-file, unresolved, don't plan off either. **M-numbers are the ONLY mode numbering** (a 3rd scheme got `roadmap.md` archived).
🗺️ **GOOD ROOM MAP: `~/house_map_v4.db`** (bag `~/map_run_20260809_185011`, 0 drops) — shape/objects/floor correct, **walls SINGLE**, scale tape-verified. `rtabmap_localization.yaml` points at it.
✅ **08-16 retest PROTOCOL** (camera restarted, colour 24.9 Hz verified, vision stopped, quiet 6 min) — **correct, reuse it.** → autonav_reference §14
✅✅ **`erpm_to_ms` = 0.003900 — CLOSED 08-13, TAPE-VALIDATED 08-12** (Y +3.2%, X −5.8%: opposite ⇒ **NO global scale factor**). ⛔ **NEVER RE-OPEN THE SCALE.** 🔑 **THE DEFECT IS THE ESC ZERO-DROPOUT** (~24% of the 23.3% crawl under-read; scale ~0%). ⚠️ **a hand spin measures SLIP-FREE — a UNITS TRAP.** ⛔ `odom_scale_measure.py` UNUSABLE AT CRAWL. → autonav_reference §5/§10/§13
⛔ **RECORD `/fmu/out/esc_status` IN FUTURE BAGS** — without it odometry can only be rescaled, not recomputed.
🔬 **DISPROVEN 08-09, don't re-propose:** map coverage · room light/auto-exposure (⚠️ but tested in DAYLIGHT — "a dark room hurts matching" still open) · rover inside the 0.308 m depth min · camera-gyro rate-dependent scale (was **my probe's arrival-time integration**) · `yaw_source: wheels` (skid-steer scrub ⇒ wheels cannot observe rotation).
🔴 **FC HEADING IS UNUSABLE — 2 UNTESTED SUSPECTS.** It is the **EKF's FUSED YAW, not the gyro**: internal **`bmm350`** (`EKF2_MAG_TYPE=1`, the only yaw observer indoors) · **GPS** (`EKF2_GPS_CTRL=7`; ⚠️ rate CONTRADICTED 3.3 vs 0.0 Hz). Test each separately. **Rover-only; the DRONE shares this FC and needs both.** → autonav_reference §6
⏭ **QUEUED:** bridge `vehicle_angular_velocity` to DDS (commented out at `dds_topics.yaml:60`; 3 fused IMUs @398 Hz, would **decouple odometry from the camera**) — needs a FC flash, so pair it with other FC work. Camera roll **1.54°** still uncorrected (§4).

✅✅ **S1 (kill switch in AutoNav) PASSED** 07-22, re-confirmed 08-10 — `tools/s1_kill_test.py`. → `autonav_reference.md` §12/§13.
🔑🔑 **EVERY AUTONAV RUN NEEDS `rover-ekf-bridge` STARTED FIRST** (FLOOR only — limit-cycle hazard is wheels-UP; **stop it after**). Indoors it is the ONLY source of the local VELOCITY estimate AutoNav requires. Signature without it: **mode refused ARMED but accepted DISARMED**. → `setup_manual.md` §C10

✅ **REFLEX COLLISION-STOP: fail-open FIXED 08-10; ALL FOUR validations PASS.** The 08-10 contact was a RATCHET on a FRESH-BUT-EMPTY scan — the threshold was never wrong. Fix = perception-health gate BEFORE the clearance test (`min_valid_fraction`=0.35). 🔑 **CUE: `BLOCK forward (BLIND)` + `scan_fresh=yes`.** → autonav_reference §8/§10/§12/§13
🔴🔴 **THE ROVER CONTACTED THE WALL 08-12 — I DROVE IT OUTSIDE ITS STOPPING ENVELOPE** (commanded 0.25 ⇒ actual ~0.9; coast ~0.30 m). NOT the reflex's fault — I sized the run off the COMMANDED speed. ⛔ **GATE EVERY MOVING TEST ON MEASURED SPEED, NEVER THE COMMAND.** ⛔ odom travel backstop USELESS at crawl — use `--min-bumper`.
🔴 **SPEED COMMANDS ARE NOT HONOURED — SAFETY-CRITICAL.** 0.05→**0.140** · **0.25→~0.9**; floor **~0.14 m/s**. ⛔ **T2 at crawl only.** ✅ **THE FLOOR = `RO_SPEED_TH` (0.10)** — firmware zeroes feedback below it ⇒ the loop re-accelerates; **structural.** ⛔ **RETRACTED "`RO_MAX_THR_SPEED` understated 7.4×" — CLOSED-LOOP DATA CANNOT IDENTIFY A PLANT GAIN; needs an OPEN-LOOP sweep.** → autonav_reference §10/§13
✅ **STANDOFF n=3 TAPE-CONFIRMED 0.345 m — SPEED-BOUND, set by the COAST not the threshold** (at ~0.9 m/s it left 0.020 m and CONTACTED). ✅ **`/scan` scale 0.9845, `front_overhang` 0.337**; reads SHORT ⇒ reflex fires EARLY (safe). ⚠️ **read autonav_reference §5/§12/§13 BEFORE any standoff measurement — 2 traps.**
🔴🔴 **`si_motor_poles` ↔ `erpm_to_ms` = LINKED PAIR — "fixing" poles in VESC Tool SILENTLY HALVES `/odom`, straight into PX4's speed loop.** VESC sends **MOTOR rpm — pole-corrected, NOT gear-corrected**; DIRECT-DRIVE hub, no gearbox. 🔑 **ADDR: 10=right-front (INVERTED) · 11=front-left · 13=rear-left · 12=right-rear.** → autonav_reference §5/§10
🔴🔴 **08-16 I KILLED THE GCS MAVLINK LINK — ONE `mavlink_shell.py` PER FC BOOT, NEVER RETRY AN EMPTY RESULT** (sessions leak; the FC stayed ALIVE, only its MAVLink instance died — DDS healthy, NO fault log). **Recovery = reboot over DDS.** → `mavlink_shell_session_exhaustion`
🔴🔴 **TRAPS THAT WILL BITE AGAIN:** a **DISARMED external mode reads as SELECTED but is NOT RUNNING** (FALSE-PASS GENERATOR) · **never quote 'last scan → wheels stopped' as reflex latency** · **`/scan` does NOT come back after a scan-killing test and `is-active` LIES.** ⛔ **Never read a quiet topic as evidence — prove a NON-ZERO baseline first.** → autonav_reference §10 · setup_manual §C3
❓ **Open: WHY the wall vanished from the scan** (3 candidates) — the gate holds regardless. → autonav_reference §10
⏭ **S2b** (kill `rover-camera` ⇒ loses `/scan` AND the heading gyro together) is separate and harsher than S2 floor.

## [TODOS] → memory/todos.md
**⏭ NEXT → `todos.md`: (a) 🔴 ESC ZERO-DROPOUT** (~24%, per-ESC) · (b) **GATE SAFETY ON `/scan`, NOT `/odom`** · (c) FC hardfaults (below) · (d) **OPEN-LOOP sweep** for `RO_MAX_THR_SPEED` · (e) localization COMMIT `map→odom`? (gates M3) · (f) T2 at crawl on MEASURED speed · (g) S2b · (h) VESC `si_motor_poles` ×4. ⛔ **`erpm_to_ms` CLOSED.** #21 gyro-yaw open.
🔴🔴 **FC HARDFAULTS = STACK EXHAUSTION, dominantly `uxrce_dds_client` (08-19, logs finally read: MPU guard hit cfsr 0x82, stack 7872/7872 FULL; 5 of 12 logs are that task, 11/12 stacks full).** 🔑 **hash `f0889f3d` in a fault log = "2.1.0 IN DISGUISE" (dirty-tree build, stale version string; source UNRECORDED).** ⛔ **2.0.0 (`a52c38b`, ~2 months clean) faulted 4× in 50 min 08-18/19 ⇒ trigger ENVIRONMENTAL.** ✅✅ **08-19: EVERYTHING ELIMINATED EXCEPT ONE — 3 FC boards, both SD cards, rolled-back firmware, restored VESC (mask odd since Feb), clean rails ALL fault.** 🔑🔑 **SOLE SURVIVOR: COMPANION DEBUG-INTERACTION (FTP bursts/shell/DDS session churn) — 10/10 faults inside interaction windows; campaign start 08-15/16 = fault onset; absent in the 2 clean months (§16.3 only cleared STEADY traffic). GO QUIET during any FC soak.** Upstream matches: PX4 #22160, #27463/PR#27108. 🔑 Fault #10 = CORRUPTION signature (healthy stack, trashed regs) ⇒ stack-full readings are symptoms; stack-raise demoted. 🔴 ALWAYS re-list+pull-verify per file before card deletes (I lost 4 logs off a stale listing). **Puller: `~/ros2_ws/tools/ftp_pull_faults.py`** (trailer `END Fault Log` UPPERCASE). ⚠️ **`microxrce-agent` LEFT STOPPED — START IT before DDS work.** ⏭ **NEXT: soak verdict → re-challenge burst (agent + FTP loop, fault ≤45 min expected) → if confirmed, throttle/serialize FC debug access.** → **`fc_hardfault_analysis.md` §17.9-17.11 — READ IT.** §12 CAN-load (RTData bit 0 still on, 2594.8 frames/s) stays true but is MARGIN, not the killer. 🔑 **fault filenames UTC (IST=+5:30)** · **trailer is `END Fault Log` (uppercase — backup tool's marker never matches)** · ⛔ **NEVER SWAP THE FC** · 🔑 **hardfault ALWAYS writes `fault_*.log`** · 🔴 `fc_fault_backup.py` broken ×2 (`list_result` + trailer). Working MAVFTP puller: scratchpad recipe in §5/§17.1. → **`fc_hardfault_analysis.md` §17 — READ IT.**
⛔ **COMPANION IS NOT SPAMMING THE FC — RE-MEASURED 08-18, STOP RE-CHECKING.** Only `mavlink-routerd` (QGC path); no pymavlink/MAVProxy/MAVSDK/MAVROS/leaked shell/cron. On DDS only `autonav_mode` publishes `/fmu/in/*` — 0 msgs/12 s on every setpoint. → §16.3
🔧 **PX4 PARAMS → `docs/px4_param_audit.md` + `setup_manual` §A7. PULL FROM THERE.** 🔴🔴 **08-14 MY PARAM CHANGE CAUSED A HARD WALL HIT IN MANUAL — `RO_ACCEL_LIM`/`RO_DECEL_LIM` SLEW THE MANUAL STICK, so CENTRING IT NO LONGER STOPS THE ROVER. KEEP BOTH AT −1.** ⚠️ **`set_param.py` writes RAM ONLY — finish with `param save`.** 🔑 **autopilot is `1:1`; NEVER read vehicle state from the GCS at `255:190`.**
1. Relay NTP · 2. 🔴 **NO onboard Wi-Fi fallback — recovery is WFB → relay:2222 ONLY** · 14. 🔶 **PAT: SSH done 08-09; STILL MUST REVOKE THE TOKEN + check `codex-relay`**
3+4+24. ❌ **DELETED, don't re-propose:** GS `rx_ring_size`, GS TX power (maxed), trim PX4 MAVLink rates.
5+7+8+9+10. Antenna tracker HW · /scan → Nav2 · #17 delete `camera_sw_node_obsolute.py` (+`rov_collision_stop`, `collision_manual_mode`) · Multicam Phase D (#25 **still keys `/dev/video0`**)
27. ⏭ Reflex `/scan_3d` is a PARAM (`collision.scan_topic`), still defaulting to `/scan`. **Needs ONE low/overhanging object `/scan` misses before flipping.**
**[OUTDOOR] O1-O5:** STL-19 · DroneCAN GPS · lidar SLAM · GPS Nav2 · outdoor safety. **WFB parked: #22 = HARDWARE, reseat drone NIC-A ant0.**
⚠️ **`align_mode:=HW` untested** — would kill the depth-glitch class, save 71%/core. ⚠️ **Orbbec clone is GITIGNORED — a re-clone RESTORES the #26 bug** (patch `codex-work 16665f5`).

## [FPV CAMERAS] — not in the manuals (autonomy camera is in setup_manual C2)
Both FPV-capable, port 6-2, **swap from QGC only**; **LG Smart Cam is the current FPV cam**. Full `usbcam-*` IDs: `vision_streaming.md`.
⛔ **Never key a camera by `/dev/videoN` or by-id** — only `usbcam-<vidpid>-<serial>-i<iface>`; Orbbec nodes vanish with `rover-camera`, and `rpivid`/`pispbe-*` renumber them.
**⚡ FPV video costs `/scan` 28.4→22.3 Hz.** ⛔ **Do NOT modify the ffmpeg command line** (vetoed). `vision_streaming` STOPPED since 08-08. Detail: `vision_streaming.md`.
## [WFB_NG] → reference_wfb_ng.md (**PARKED — only action left is HW: reseat drone NIC-A ant0**)
**Drone TX is flawless — when video breaks WFB's input queue is EMPTY, SUSPECT THE SOURCE.** ⚠️ **METHOD (cost a week): compare payload `tx.incoming`→`rx.out` across TCP 8102/8103 JSON (not `wfb-cli`) · never `rx.all` (double-counts 4 antennas) · never infer radio health from MAVLink rates at `tcp:5760`.** Addresses/keys/channel: `reference_wfb_ng.md`.
## [RELAY_STATION] → `relay2_relaystn`
`vind-rly` RPi5 | `ssh vind-admin@10.5.5.77` (**sudo NEEDS A PASSWORD ⇒ journalctl of other units = "No entries"**) | `~/codex-relay` | tunnel 2222→drone :22 | **NO RTC**. Services, Wi-Fi Direct/QGC addressing: `project_relay2_relaystn.md`.
## [REPOS / GCS]
`codex-work`: `~/codex-work` → Companion_Computer_Pxlabs, branch **master** (origin/main stale) | `codex-relay`: on vind-rly → Relay_Station_Pxlabs | `ros2_ws`: `~/ros2_ws`, SSH ✅, ⚠️ **currently on `fix/collision-perception-health-gate`, not main — check the branch before assuming**
GCS: G-Control.exe → pxlabs_cli.exe → SSH relay:2222 → companion:22 | QGC: ArvinVeiyon/PXLABS_qgroundcontrol @ PXLABS-integration
🔴 **PAT STILL LIVE ON GITHUB — REVOKE IT** (github.com/settings/tokens). Remotes moved to SSH 08-09, which stopped it being USED, not rotated. ⏭ `~/codex-relay` never checked for the same pattern.
## [SERVICE / TROUBLESHOOTING QUICK INDEX] → full detail in setup_manual C, D2, E5
**AIDE timer DISABLED** (~3.5 h/day of a core). **`tfmini` DISABLED — ⚠️ MUST `systemctl enable --now tfmini` for the DRONE.** **`rover-ekf-bridge` DISABLED on purpose** (wheels-up limit-cycle hazard); velocity-only ⇒ **the Q1 hook is half-built.**
no_MAVLink → `ttyAMA0` + the PX4 MAVLink instance; 🔑 **GCS lost MAVLink but DDS FINE ⇒ FC ALIVE, only its MAVLink instance died — reboot over DDS** | no_DDS → `microxrce-agent`, `ttyAMA4` | WFB_down → `wifibroadcast@drone` | **no_video → CPU-starvation latch FIRST** | no_scan/no_cloud → **half-dead camera restart** | localization degrading → **measure colour rate, restart camera**
