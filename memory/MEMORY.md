# Vind-Roz Platform Memory
> Auto-loaded each session; also the Phi-3 offline prompt. Live: `~/.claude/projects/-home-roz/memory/` | Backup: `~/codex-work/memory/` → ArvinVeiyon/Companion_Computer_Pxlabs
> ⚠️ **BACKUP IS MANUAL:** `cp -p .../memory/*.md ~/codex-work/memory/` + push. **Never `rsync --delete`** — the mirror is a UNION of 2 scopes; the 2nd (`-home-roz-codex-work`) is NOT auto-loaded.
> ⚠️ **KEEP UNDER 125 LINES / 17 KB.** One line per entry. **Technical detail belongs in a topic file or a manual — leave a POINTER here, not the fact.**

## 📘 THE TWO MANUALS — READ THESE BEFORE ASKING THE OPERATOR OR DERIVING ANYTHING
📐 **`~/ros2_ws/docs/autonav_reference.md`** — *what is TRUE and why.* **§5 constants WITH THEIR METHODS · §10 known faults · §11 inviolable rules · §12 test ladder · §13 DATED FITNESS.**
📕 **`setup_manual.md`** — *what to DO.* **§C NODE INVENTORY (real ExecStart+params; §C10: an external mode's requirements come from the SETPOINT TYPE) · §D6 arming AutoNav · §E2b map verification · §A7 param changelog (`RO_*` rover-only vs `EKF2_*` SHARED WITH THE DRONE)**. ⚠️ Part A mostly UNVERIFIED.
🔑 **A bare `§N` below = `autonav_reference`. §5 before trusting any constant · §11 before designing behaviour · setup_manual C before touching a node · E before running a procedure.**
📗 **`docs/README.md` = THE DOC INDEX, open it.** Traps: **`autonomy_plan.md` M0-M4 = the ONLY mode numbering** · ⚠️ `ros2_architecture.md` STALE · 🗄 `roadmap.md` archived. 🔑 **08-10: the doc that disagreed with three others was the accurate one — count of sources ≠ weight of evidence.**
🔑 **Arming AutoNav: external mode, NO RC switch slot** ⇒ RC arm always lands in **Manual**. Arm in Manual (throttle neutral) → companion sends `DO_SET_MODE main=4 sub=11`. ⛔ **never arm AutoNav on stands with the EKF bridge running** (self-sustaining limit cycle; only a disarm stops it). → setup_manual §D6

## [MEMORY_FILES]
**feedback (RULES) — 🔴 OPEN THE FILE, THE ONE-LINER IS NOT THE RULE:** `defer_memory_updates` **(WORK FIRST, BATCH MEMORY WRITES TO THE END)** · `debug_before_documenting` · `scope_px4_params_by_control_flags` **(`flag_control_*_enabled`, NOT the doc string — put the rover into a wall)** · `verify_after_editing` **(grep TWICE: the OLD CLAIM *and* what SURVIVED)** · `test_before_concluding` · `notify_before_recording` **(OPERATOR IS THE INSTRUMENT — GET A GO)** · `log_the_discriminator` **(record what settles it; NEVER log a SUGGESTED condition as an OBSERVED one)** · `independent_rulers` · `check_docs_before_measuring` · `eliminate_hypothesis_whole_family` · `crash_recovery_checkpoint` · `use_dds_not_mavlink` · `mavlink_shell_session_exhaustion` · `camera_qgc_only` · `dkms_arch` · `wlan0_persistent_name` · `answer_short` **(STATUS = ONE LINE)** · `explain_in_prose` **(EXPLAINING ⇒ plain connected sentences; NO tables/`file:line`/bold-shrapnel — those are for DOCS)**
**reference:** `px4_vio_collision` (🔴 **MC-ONLY, dead on a rover; no PX4 path planner**) · `this_machine` · `wfb_ng` · `wfb_rlyctl` · `wfb_cfg_apply` · `uart_map` · `services` · `known_fixes_archive` · `gcs_companion_interface` · `todos.md` · `ros2_nodes`/`ros2_topics` · `rover_odometry`
**project — ACTIVE:** `indoor_mapping_slam` (§13-17) · `rover_autonav` · `perception_3d_costmap` · `autonomy_plan_reframe` · `vesc_can_flashing` (**NOW A POINTER FILE**: 🔴 **`60_mk5.bin` OVER DRONECAN = BRICK, USB ONLY · VESC Tool over CAN IMPOSSIBLE** ✅ **09-09 RC BRAKE DONE — all 4 flashed, PX4 saved, BENCH-TESTED & RECORDED** 🅿️ **COMPANION CAN INTEGRATION DROPPED, overlay off — `codex-work/companion_can_driver_status.md`; ⛔ don't re-debug the MCP2515** ⏭ **`codex-work/bldc_can/RESUME.md` + `evidence/brake_bench_test_20260909.md`**)
**project — other:** `fc_hardfaults` · `rc_ch10_reboots_companion` · `l4_gemini_nav2_prereqs` · `vision_multicam_upgrade` · `l2_floortest_wheel0_reversed` · `wfb_undervoltage_dead_nic` · `external_wifi_uplink` · `gcs_link_degraded` · `relay_ntp_setup` · `relay2_relaystn` · `companion_network_degraded` · `boxb_pcie_usb` · `codexrelay_divergence` · `codexwork_token_in_remote` (🔴 PAT) · `codexwork_branches`

## [WORKING ON THIS MACHINE] — things the manuals do NOT cover
⚠️ **sudo NEEDS the password:** `printf '1987\n' | sudo -S …`
⚠️ **`pkill -f <pat>` MATCHES MY OWN SHELL** (⇒ self-kill, exit 144). Use `pgrep -x` / `pgrep -f "[p]attern"` then `kill <pid>`.
🔴 **I AM A MAJOR CPU CONSUMER — BUDGET FOR IT:** `claude` runs **55-86% of a core**. Power check = `vcgencmd get_throttled` (0x0 clean).
✅ **I can read/write PX4 params myself:** `python3 ~/ros2_ws/tools/set_param.py NAME [value]`. **Don't ask the operator to read QGC.**
📄 **Measurement hygiene · `cpu_catcher.sh` · "prove the DETECTOR was live too" → `this_machine`.** 🔴 **EVERY NIC NAME/SYSPATH IN THE DOCS IS STALE — the uplink ADAPTER was swapped (`lsusb` first).** ⚠️ **Boot clock WRONG until NTP steps it.** ⚠️ **Replays = `ROS_DOMAIN_ID=42`, live = 0.**

## [CURRENT STATE — ARMED SUCCESSFULLY 08-10 (S1); the old "NEVER ARMED" is WITHDRAWN]
✅ **CAN IS BACK — MEASURED 08-21** (`esc_count 4`, flags 15; `UAVCAN_ENABLE=3`) ⇒ **"CAN GONE / rover IMMOBILE" WITHDRAWN, todos (a)(d)(f)(h) UNBLOCKED.** ⚠️ **flags=15 is BUS PRESENCE, not motion.**
🛑 **STOP CALIBRATING `erpm_to_ms` (operator, 08-13) — right call, wrong reason: one constant IS right, the defect was never the scale.** ⇒ **gate safety on `/scan` CLEARANCE, not `/odom`.**
✅ **`ros2_ws` on `main`, pushed & synced (09-05 @ `d562b88`)** — old "NOT main" trap WITHDRAWN. ⚠️ **`nav_state` may still read 23 (AutoNav, inert) — clear it on the RC first.**
🔑 **THERE IS NO VIO, AND NEVER WAS** — position = wheel ERPM + camera gyro dead reckoning; **RTAB-Map CONSUMES our TF odom** and adds `map→odom`, like AMCL. **Don't propose adding VIO.** → reference §6
🔑 **PX4 NEVER KNOWS WHERE IT IS, BY DESIGN** — the bridge sends VELOCITY only; **position lives in ROS, not the FC.** ⚠️ **always state the bridge's state when quoting `v_xy_valid`** (false stopped, true running).
🔴 **LOCALIZATION DEAD — ZERO ACCEPTED.** 🔑 **`map→odom` CHANGING is NOT health — ADJUDICATE ON THE ACCEPTED-FIX COUNT.** ✅ **09-04 replay of the map's OWN bag: 0/20 ⇒ GENUINE PIPELINE FAULT, not viewpoint; fails at GEOMETRY (inliers 17×0, best 9, thr 12), NOT appearance.** ❌ **DEAD: `camera_info` · map data · live depth.** ⛔ **NEVER lower `Vis/MinInliers`.** ⛔ DON'T MAP THE HOUSE FIRST. **🔴 `fx 409.85 @848×480` is STALE — real 304.05 @640×360.** → `indoor_mapping_slam` §17
✅ **2D GRID IS FINE (08-16, `tools/grid_review.py`)** — ⛔ **Do NOT "fix" it:** `MaxGroundHeight` 0.28 = 3× truth, **keep 0.10**; `NormalsSegmentation false` = the spikes that got v5 rejected.
🔴 **THE REAL CONSTRAINT IS THE ROOM: ~2 m² open floor vs a 0.73×0.56 m rover.** ⛔ **T2 CANNOT RUN HERE** — needs ~3.05 m clearance, aborts before moving. ⏭ operator: corridor (recommended) · re-scope · or drop this room as M3.
✅ **"uXRCE UART REBOOTS THE COMPANION" = RC CH10, NOT A FAULT** — `rc_control_node` reads CH10 off `/fmu/out/input_rc`: **2014=reboot · 1514(MIDDLE)=shutdown · 1011=down=safe.** 🔑 **CHECK CH10 BEFORE debugging any companion restart or rebooting the FC.** → `rc_ch10_reboots_companion`
⛔⛔ **THE CAMERA IS MOUNTED CORRECTLY. STOP RAISING "CAMERA ROTATED" — THE OPERATOR HAS CORRECTED ME REPEATEDLY AND HE IS RIGHT** (again 09-11: a −3.9° `/scan` bearing was **THE PARKING**, not the mount — he squared THE ROVER, never touched the camera, and it collapsed to −0.25°). **Mount re-measured 09-09: pitch 1.436°, roll −0.447°, sd 0.006, 12145 samples** (`tools/cam_mount_probe.py`, gravity on `/camera/accel/sample`) = a normal level mount. **The stale thing was the LAUNCH FILE — corrected 09-10 → `cam_pitch` 0.0251 / `cam_roll` −0.0078**, made live by the 09-11 boot. ✅✅ **09-11 G0 CLOSED, VERIFIED AT A WALL: 5 parkings, fit RMS 0.3-3.4 mm, inlier fraction 1.00 at EVERY range ⇒ NO FLOOR IN THE SCAN, coverage 0.80 vs 0.35. DECISIVE: taped 1.12 m bumper→wall, `/scan` read 1.4390 vs 1.4344 predicted = 4.6 mm ⇒ `front_overhang` 0.337 AND scale 0.9845 BOTH SURVIVED THE TRANSFORM CHANGE — ⛔ DO NOT RE-DERIVE THEM.** 🔑 **`cam_yaw` 0.0 CONFIRMED, not assumed — gravity CANNOT observe yaw, so `cam_mount_probe` structurally cannot check it; only `wall_probe` can.** ⚠️ **`ros2_ws` on `main`: UNCOMMITTED** + untracked `tools/cam_mount_probe.py`. → `project_rover_autonav` 09-11
⛔ **09-04 `UAVCAN_EC_MIN1..4`=110 / `MAX1..4`=8082 ARE DELIBERATE — NEVER "tidy" them back to 10/8191** (sum 8192 ⇒ neutral exactly 4096; 10 = dead full-reverse). 🔴 **CORRECTED 09-11: `esc_errorcount` is NOT a fault code — `EscReport.msg` calls it "Number of reported errors by ESC", a CUMULATIVE COUNT. "51" = 51 errors, NOT code 51.** The fault IDENTITY is the `failures` uint16 BITMASK (OVER_CURRENT / OVER_VOLTAGE / MOTOR_OVER_TEMPERATURE / OVER_RPM / INCONSISTENT_CMD / MOTOR_STUCK / GENERIC / temp warns). 🔑 **`EscReport[8] esc` is ALWAYS 8 long — only `esc_count` + `esc_online_flags` say which entries are real.** 🔑 **`input_rc`/`manual_control_setpoint`/`esc_status` are ON DDS ⇒ diagnose ESCs with NO VESC Tool and NO `mavlink_shell`.** ⚠️ **BENCH ONLY — stands, unloaded, NEVER DRIVEN.** → **`codex-work/rc_configuration.md` + `px4_vesc_dronecan_implementation.md`** (moved OUT of `ros2_ws` 09-04; **`setup_manual` §A7 stays in `ros2_ws` and is still the canonical param changelog**)
✅ **09-04 LADDERS SETTLED: M0-M4=goal · L0-L5 (App.A+B, NOT duplicates)=capability · R1-R7=reqs; 🗄 requirements §4 L0-L7 RETIRED.** ⏭⏭ **THE PLAN = `todos.md` §REQUIREMENTS REALIGNMENT, gates G0-G7.**
🗺️ **GOOD ROOM MAP: `~/house_map_v4.db`** (bag `~/map_run_20260809_185011`) — shape/objects/floor correct, **walls SINGLE**, scale tape-verified. `rtabmap_localization.yaml` points at it.
✅ **08-16 retest protocol — reuse it.** → §14
✅ **ODOMETRY: `erpm_to_ms`=0.003900 CLOSED, tape-validated — NEVER RE-OPEN THE SCALE.** 🔑 the defect is the **ESC ZERO-DROPOUT** · 🔴 **`si_motor_poles` ↔ `erpm_to_ms` = LINKED PAIR — "fixing" poles in VESC Tool SILENTLY HALVES `/odom`** · ⚠️ a hand spin measures SLIP-FREE (units trap) · **ADDR 10=RF(INV) 11=FL 13=RL 12=RR** · ⛔ **RECORD `esc_status` IN BAGS.** → §5/§10/§13
🔬 **DISPROVEN 08-09, don't re-propose:** map coverage · light/auto-exposure (⚠️ tested in DAYLIGHT — "dark room hurts matching" still open) · rover inside the 0.308 m depth min · camera-gyro rate-dependent scale · `yaw_source: wheels` (skid-steer scrub).
🔴 **FC HEADING IS UNUSABLE — 2 UNTESTED SUSPECTS.** It is the **EKF's FUSED YAW, not the gyro**: **`bmm350`** (`EKF2_MAG_TYPE=1`, the only indoor yaw observer) · **GPS** (`EKF2_GPS_CTRL=7`). Test separately. ⚠️ **the DRONE shares this FC and needs both.** → §6
⏭ **QUEUED:** bridge `vehicle_angular_velocity` to DDS (commented out at `dds_topics.yaml:60`; 3 fused IMUs @398 Hz, would **decouple odometry from the camera**) — needs a FC flash, pair with other FC work.

✅ **S1 kill-switch PASSED** 07-22, re-confirmed 08-10 (`tools/s1_kill_test.py`) → §12/§13.
🔑🔑 **EVERY AUTONAV RUN NEEDS `rover-ekf-bridge` STARTED FIRST** (FLOOR only — the limit-cycle hazard is wheels-UP; **stop it after**). Indoors it is the ONLY source of the local VELOCITY estimate AutoNav requires. Without it: **mode refused ARMED, accepted DISARMED**. → setup_manual §C10

✅ **REFLEX COLLISION-STOP: fail-open FIXED 08-10, all 4 validations pass** — perception-health gate BEFORE the clearance test (`min_valid_fraction`=0.35); the threshold was never wrong. 🔑 **CUE: `BLOCK forward (BLIND)` + `scan_fresh=yes`.** → §8/§12
🔴🔴 **SPEED COMMANDS ARE NOT HONOURED — SAFETY-CRITICAL. 0.05→0.140 · 0.25→~0.9 m/s; floor ~0.14; ⚠️ **`RO_SPEED_TH` READS 0.10 ON THE FC (09-11) — the equality is UNVERIFIED, G2****: firmware zeroes feedback below it ⇒ the loop re-accelerates — **structural.** ⛔ **RETRACTED "`RO_MAX_THR_SPEED` understated 7.4×" — closed-loop data CANNOT identify a plant gain.** ⛔ **GATE EVERY MOVING TEST ON MEASURED SPEED, NEVER THE COMMAND** (I hit the wall 08-12 sizing off the command; coast ~0.30 m); odom backstop useless at crawl — use `--min-bumper`. → §10/§13
✅ **ALL 4 WHEELS HAVE THE RC BRAKE — DRIVEN ON THE FLOOR, LOADED, MEASURED 09-09** (ch3→AUX1→`UAVCAN_EC_FUNC5` slot 5→idx 4): **0.69 m/s², STOPS IN 0.30-0.50 m FROM ~0.8 m/s**; proportional `aux1` CONFIRMED, `RC3_TRIM` still 1487.5, `esc_errorcount` 0 under load. ⚠️ **coast baseline is n=2 ⇒ the 3.4× and the "saves 1.4 m" are PROVISIONAL.** → `evidence/brake_floor_test_20260909.md` (+`_bench_` = run 1) · procedure = **`rc_configuration.md` §6**. 🔴 **THE REFLEX STILL ONLY ZEROES THE SETPOINT — IT NEVER COMMANDS THE BRAKE.** ⛔ **ZERO THROTTLE = FREE-FLOW COAST, NOT BRAKING** (regen ⇒ ~zero torque at standstill) — that is WHY the standoff is speed-bound and why it CONTACTED the wall at 0.9 m/s. Wiring the brake in is the unmade change; 0.69 vs 0.20 m/s² is the argument. ⚠️ **S1's "<20 ms to wheels zero" MAY BE THE ESC ZERO-DROPOUT reporting 0 rpm while the wheel still turns — disarm CANNOT physically stop a rolling rover in 20 ms. Re-measure against `esc_current` when S1 is re-run.**
🔑 **09-11: `esc_count` IS NOW 5, NOT 4 — SLOT 5 (idx 4) IS THE BRAKE** (`UAVCAN_EC_FUNC5`). `esc_armed_flags` 31, `esc_online_flags` **15** — the brake is armed but NEVER online, because a brake output is not a telemetry ESC. ⇒ **QGC's "ESC 5 not connected" ERROR IS EXPECTED, NOT A FAULT — but it PERMANENTLY LIGHTS QGC's ESC health, so ⛔ QGC CAN NO LONGER DETECT A REAL DISCONNECT. Read `esc_online_flags` BIT-BY-BIT over DDS instead.** ⛔ **NEVER index `esc[]` BY POSITION — key on `esc_address` and drop `timestamp == 0`** (fixed 09-11 in `l2_test.py` + `yaw_response_log.py`; the other tools already did it right).
🔑 **`esc_current` IS THE LOADED/UNLOADED RULER** (±1 A at rest vs −12…+8 A moving) **and `/odom` DRIFTS AT STANDSTILL** (~0.38 m/min at 0 rpm, camera-gyro term) ⇒ corroborate with IMU `sensor_combined`, never trust `/odom` alone. ⛔ **STANDOFF 0.345 m (n=3, tape): the "~0.30 m coast at 0.9 m/s" is DISTANCE-BEFORE-CONTACT (it left 0.020 m and CONTACTED), NOT a stopping distance — NEVER compare it to a braking figure.** 🔑 **REGENERATIVE ⇒ ≈ZERO torque at standstill, a HAND TEST CANNOT MEASURE IT**; holding brake = `set_handbrake_rel`. ⚠️ `timeout_brake_current`=2 A on 300 ms command loss. **`/scan` reads SHORT ⇒ reflex fires EARLY (safe).** ⚠️ **§5/§12/§13 first — 2 traps.** → `vesc_can_flashing`
🔴 **08-16 I KILLED THE GCS MAVLINK LINK — ONE `mavlink_shell.py` PER FC BOOT, NEVER RETRY AN EMPTY RESULT** (sessions leak; the FC stayed ALIVE, only its MAVLink instance died — DDS healthy, NO fault log). **Recovery = reboot over DDS.** → `mavlink_shell_session_exhaustion`
🔴 **TRAPS THAT WILL BITE AGAIN:** a **DISARMED external mode reads as SELECTED but is NOT RUNNING** (false-pass generator) · **never quote 'last scan → wheels stopped' as reflex latency** · **`/scan` does NOT come back after a scan-killing test and `is-active` LIES.** ⛔ **Never read a quiet topic as evidence — prove a NON-ZERO baseline first.** → §10

## [TODOS] → memory/todos.md
⏭⏭ **FOCUS (operator): AUTONAV.** 🧹 **`todos.md` PRUNED 09-10, 793→576 lines — the plan is its REQUIREMENTS REALIGNMENT + START HERE block, nothing above them.** 🔑 **RUN THE GATES IN DEPENDENCY ORDER `G0→G2→G3`, NOT numerically — M2 needs NO map and NO localization, so G1 gates only M3 and must not sit in front of M2** (`autonomy_plan.md` §5 says so outright). **G0** ✅✅ **CLOSED 09-11 — verified at a wall, constants survived** (`project_rover_autonav`). **G2** = `RO_MAX_THR_SPEED` sweep · ESC zero-dropout · **gate safety on `/scan`, NOT `/odom`**. **G3** = ⏭ **OPERATOR VENUE CALL (corridor?) — T2 blocked by the ROOM.** ⛔ HARDFAULT PURGE DROPPED · ⛔ `erpm_to_ms` CLOSED.
✅ **FC HARDFAULTS CLOSED 08-29 — ⛔ DON'T REOPEN.** 🔴 **ONLY LIVE HAZARD: the fw is OUR OWN BUILD (NXP fix PR #28141 cherry-picked), so ANY reflash from upstream or `~/fc_firmware` BRINGS THEM BACK — verify by GIT HASH, not `flight_sw_version`.** → `HARDFAULT.md` · `fc_hardfaults`
🔧 **PX4 PARAMS — READ THE FC, never trust a snapshot; values + RCA → `px4_param_audit.md` + `setup_manual` §A7.** 🔴 **KEEP `RO_ACCEL_LIM`/`RO_DECEL_LIM` AT −1: they SLEW THE MANUAL STICK, so centring it no longer stops the rover (my 08-14 change = a hard wall hit).** ⚠️ `set_param.py` = MAVLink `tcp:5760` (**not DDS**), **RAM ONLY, FLOAT-ONLY** 🔴 **INT32 params need `bldc_can/diag/set_param_int.py`; save = `diag/param_save.py`; FC reboot over DDS = `diag/fc_reboot.py`. Tool table + the whole RC setup procedure = `rc_configuration.md` §6.** ✅ **MAVLink BACK UP 09-10** (was down 09-09 across an FC reboot; came back on its own). 🔑 **`<no reply>` from `set_param.py` = WRONG NAME far more often than "busy" — PROVE IT WITH A FAKE-NAME CONTROL** (that is how `RC2_DZ`/`RC3_DZ` were settled 09-10: they DO NOT EXIST). ⛔ **NEVER the `2026-05-29` file.** ⚠️ autopilot is `1:1` — **never read state from the GCS `255:190`.** 🔴 **3 fault logs still ON THE CARD ⇒ QGC re-announces them EVERY BOOT.**
🔑 **BOOT-CLOCK TRAP: this box's journal restamps early boot, so `ExecMainStartTimestamp` + `NRestarts=0` FORGED a convincing "up 11 h, zero reboots" — it was 4 MINUTES old. CHECK `/proc/uptime` BEFORE READING ANY DURATION OFF A TIMESTAMP.**
**Numbered backlog → `todos.md`** (⚠️ **`5. Antenna tracker HW` is ONLY here**; the ❌DELETED 3/4/24 items are recorded there). Traps: 🔴 **NO Wi-Fi fallback — remote = WFB → relay:2222**; ⚠️ **09-03 eth0 WIRED: `ssh roz@10.10.10.10` (laptop .20/24), UNTESTED** → setup_manual §E5b · ⏭ **27:** reflex `/scan_3d` is a PARAM (`collision.scan_topic`), still `/scan` — **needs ONE low object `/scan` misses** · **#25 still keys `/dev/video0`**.
**[OUTDOOR] O1-O5:** STL-19 · DroneCAN GPS · lidar SLAM · GPS Nav2 · outdoor safety. **WFB parked: #22 = HARDWARE, reseat drone NIC-A ant0.**
⚠️ **`align_mode:=HW` untested** — would kill the depth-glitch class, save 71%/core. ⚠️ **Orbbec clone is GITIGNORED — a re-clone RESTORES #26** (patch `codex-work 16665f5`).

## [FPV CAMERAS] → `docs/vision_streaming.md` (autonomy camera is in setup_manual C2)
Both FPV-capable, port 6-2, **swap from QGC only**; **LG Smart Cam is current**. ⛔ **Never key a camera by `/dev/videoN` or by-id** — only `usbcam-<vidpid>-<serial>-i<iface>`. **⚡ FPV video costs `/scan` 28.4→22.3 Hz.** ⛔ **Do NOT modify the ffmpeg line** (vetoed). ⚠️ **`vision_streaming` RUNNING ≠ streaming** — **proof method + the `wchar` trap that fakes "dead" → `this_machine`.**
## [WFB_NG] → `reference_wfb_ng.md` (**PARKED — only action left is HW: reseat drone NIC-A ant0**)
**Drone TX is flawless — when video breaks WFB's input queue is EMPTY: SUSPECT THE SOURCE.** ⚠️ **the measurement METHOD cost a week — read the file before measuring anything radio.**
## [RELAY_STATION] → `relay2_relaystn` (**all detail is there: the Pi Zero failure, host-key reset, the cluster loop, sync**)
`vind-rly` RPi5 | `ssh vind-admin@10.5.5.77` (**sudo NEEDS A PASSWORD ⇒ journalctl of other units = "No entries"**) | tunnel 2222→drone :22 | **NO RTC** | running an OLD SD card, repo @ `70ef6aa`.
✅ **08-28 CLOSED: relay MAVLink + the cluster-wipe loop.** ⛔ **No Pi Zero 2 W relay · never `wfb-rlyctl use-cluster` (no CPE610) · `systemctl mask` FAILS here, use `disable`.** 🔑 **`is-active` LIES — check `ps`/the port · ping to the QGC laptop is a FALSE NEGATIVE (use `ip neigh` + `iw station dump`) · a repo pull DEPLOYS NOTHING.**
## [REPOS / GCS]
`codex-work`: `~/codex-work` → Companion_Computer_Pxlabs, branch **master** (origin/main stale); **docs live at the ROOT, no `docs/` dir — rover/DroneCAN docs belong HERE, not `ros2_ws` (operator 09-04)** | `codex-relay`: on vind-rly → Relay_Station_Pxlabs | `ros2_ws`: `~/ros2_ws` → ArvinVeiyon/ros2_ws, **on `main`, synced 09-02**
GCS: G-Control.exe → pxlabs_cli.exe → SSH relay:2222 → companion:22 | QGC: ArvinVeiyon/PXLABS_qgroundcontrol @ PXLABS-integration
🔴🔴 **2 LIVE PATs STILL UNREVOKED (08-20): `~/git_key` (fine-grained) + a CLASSIC scope-`repo` token in `~/.claude/history.jsonl`.** ⛔ **OPERATOR MUST REVOKE IN A BROWSER — no API can do it.** ✅ Revoking breaks nothing (all remotes SSH). 🔑 **NEVER PASTE A SECRET INTO A PROMPT — cleartext in `history.jsonl`.** → `codexwork_token_in_remote`
## [SERVICE / TROUBLESHOOTING QUICK INDEX] → full detail in setup_manual C, D2, E5
**AIDE timer DISABLED** (~3.5 h/day of a core) · **`tfmini` DISABLED — MUST `enable --now tfmini` for the DRONE.** **`rover-ekf-bridge` DISABLED on purpose** (wheels-up limit cycle). ✅ **09-06: `vision_streaming` IS enabled and returns on its own across a reboot — "disabled at boot / a reboot kills the video" WITHDRAWN.** ✅ **09-04 SETTLED: the unit IS `mavlink.router.service` (active); setup_manual C8 was right, the earlier "no such unit" was a bad search.** 🔴 **09-04: all 6 units read `active` while depth/colour/`/scan` were 0.0 Hz — `active` PROVES NOTHING, measure rates.**
🔑 **`ros2 node list` GHOSTS for minutes after a stop — prove dead with `pgrep`; `echo`/`list` can return NOTHING on a live topic — retry `--no-daemon`** (🔴 09-02: not a flag on `topic hz`, and `hz`+`echo` BOTH went silent on a healthy `/scan` ⇒ **the ros2 CLI is itself an unreliable ruler** → `this_machine`).
no_MAVLink → `ttyAMA0` + the PX4 MAVLink instance (**DDS fine ⇒ FC alive, reboot over DDS**) | no_DDS → `microxrce-agent`, `ttyAMA4` | WFB_down → `wifibroadcast@drone` | **no_video → CPU-starvation latch FIRST** | no_scan/no_cloud → **camera restart THEN `rover-scan`/`-scan-3d`/`-odometry` — 0 Hz forever otherwise (09-04)** | localization degrading → **colour rate, restart camera**
