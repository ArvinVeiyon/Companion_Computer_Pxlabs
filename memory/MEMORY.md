# Vind-Roz Platform Memory
> Auto-loaded each session; also the Phi-3 offline system prompt. Live: `~/.claude/projects/-home-roz/memory/` | Backup: `~/codex-work/memory/` → GitHub ArvinVeiyon/Companion_Computer_Pxlabs
> ⚠️ **BACKUP IS MANUAL:** `cp -p ~/.claude/projects/-home-roz/memory/*.md ~/codex-work/memory/` + git push. **Never `rsync --delete`** — the mirror is a UNION of two scopes; the 2nd (`-home-roz-codex-work`) is NOT auto-loaded, check it after any codex-work session.
> ⚠️ **KEEP UNDER 125 LINES / 17 KB.** One line per entry. **Technical detail belongs in the two manuals below — put a POINTER here, not the fact.**

## 📘 THE TWO MANUALS — READ THESE BEFORE ASKING THE OPERATOR OR DERIVING ANYTHING
📐 **`~/ros2_ws/docs/autonav_reference.md`** — *what is TRUE and why.* 14 cited sections: §1 goal · §2 four questions · §3 platform+CPU · §4 geometry · **§5 constants WITH THEIR METHODS** · §6 odometry+heading · §7 motion envelope · §8 perception · §9 mapping/localization · §10 known faults · **§11 inviolable rules** · §12 test ladder · **§13 DATED FITNESS ASSESSMENT — what is fit for use, when, on what evidence** · §14 method notes.
📕 **`~/ros2_ws/docs/setup_manual.md`** — *what to DO.* A firmware/calib · B build · **C NODE INVENTORY (real ExecStart + params; §C10 PX4 EXTERNAL MODES — requirements come from the SETPOINT TYPE, not the mode)** · D bring-up (**§D6 arming into AutoNav**) · E ops (**§E2b MANUAL MAP VERIFICATION — `tools/map_review.py`, Q1 "any wall drawn twice" decides it**) · **§A7 PX4 PARAM CHANGELOG (`RO_*` rover-only vs `EKF2_*` SHARED WITH THE DRONE)** · F stubs. ⚠️ Part A mostly UNVERIFIED-ON-THIS-VEHICLE (📋).
🔑 **Read §5 before trusting any constant · §11 before designing any behaviour · setup_manual C before touching a node · E before running a procedure.** ⚠️ setup_manual Part A (FC calibration) is mostly UNVERIFIED-ON-THIS-VEHICLE (marked 📋) — correct it the next time a calibration is actually run.
📗 **`docs/README.md` = THE DOC INDEX — open it rather than duplicating it here.** Deep files: `rover_geometry.md` · `rover_yaw_response.md` · `vision_streaming.md` · `autonomy_plan.md` (modes **M0-M4 = the ONLY mode numbering**) · `rover_autonav_requirements.md` · `indoor_mapping_plan.md`. ⚠️ `ros2_architecture.md` STALE (setup_manual C supersedes). 🗄 archived: `roadmap.md` (L-number clash = hazard) · `yaw_tuning_session.md` · `third_party.md`.
✅ **SAFETY CONFLICT RESOLVED 08-10 — `rover_autonav_collision_stop.md` was RIGHT, the "S1 untested" records were STALE.** S1 passed 07-22, re-confirmed 08-10. All four records corrected. 🔑 **Lesson: the doc that disagreed with three others was the accurate one — count of sources ≠ weight of evidence.**
🔑 **Arming AutoNav: it is an external mode with NO RC switch slot** ⇒ RC arm always lands in **Manual**. Path = arm in Manual (throttle neutral) → companion sends `DO_SET_MODE main=4 sub=11`. ⛔ **never arm AutoNav on stands with the EKF bridge running** (self-sustaining limit cycle; only a disarm stops it). → setup_manual §D6

## [MEMORY_FILES]
**feedback (RULES) — 🔴 OPEN THE FILE, THE ONE-LINERS ARE NOT THE RULE:** `verify_after_editing` **after ANY deletion GREP TWICE — for the OLD CLAIM *and* for what should have SURVIVED; I skip this exactly when confident, which is when it catches something** · `test_before_concluding` **never publish a number I didn't measure; RUN the real consumer; label measured/derived/assumed; ⛔ A GREP THAT FINDS NOTHING PROVES THE PATTERN ABSENT, NOT THE EVENT** · `check_docs_before_measuring` **grep memory + `docs/` BEFORE deriving; the operator's ID of their own hardware beats my inference** · `eliminate_hypothesis_whole_family` **ONE command, name the CONSUMER** · `crash_recovery_checkpoint` · `use_dds_not_mavlink` · `camera_qgc_only` · `dkms_arch` · `wlan0_persistent_name`
**reference:** `px4_vio_collision` (🔴 **PX4 collision-prevention is MC-ONLY, dead on a rover; no PX4 path planner**) · `wfb_ng` · `wfb_rlyctl` · `wfb_cfg_apply` · `uart_map` · `services` · `known_fixes_archive` · `gcs_companion_interface` · `todos.md` · `ros2_nodes`/`ros2_topics` · `rover_odometry`
**project — ACTIVE:** `indoor_mapping_slam` (§13-16 = the 08-09 story) · `rover_autonav` (the wall-calibration record) · `perception_3d_costmap` · `autonomy_plan_reframe`
**project — other:** `l4_gemini_nav2_prereqs` · `vision_multicam_upgrade` · `l2_floortest_wheel0_reversed` · `wfb_undervoltage_dead_nic` (**DON'T raise the pot**) · `external_wifi_uplink` · `gcs_link_degraded` · `relay_ntp_setup` · `relay2_relaystn` · `companion_network_degraded` · `boxb_pcie_usb` · `codexrelay_divergence` · `codexwork_token_in_remote` (🔴 **plaintext PAT**) · `codexwork_branches` (**auto-sync does NOT git-add NEW files**) · `rc_control_camera_retry_storm` (FIXED)

## [WORKING ON THIS MACHINE] — things the manuals do NOT cover
⚠️ **sudo NEEDS the password:** `printf '1987\n' | sudo -S …`
⚠️ **Boot clock is WRONG until NTP steps it** — don't correlate journals across a boot.
⚠️ **`pkill -f <pat>` MATCHES MY OWN SHELL** (the pattern is in my command line ⇒ self-kill, exit 144). Use `pgrep -x` / `pgrep -f "[p]attern"` then `kill <pid>`.
🔴🔴 **I AM A MAJOR CPU CONSUMER — BUDGET FOR IT.** `claude` runs **55-86% of a core**. Its npm update check and the pemmican MOTD hook are **DISABLED** (`DISABLE_AUTOUPDATER=1`; `chmod -x /etc/update-motd.d/90-pemmican`). Power check = `vcgencmd get_throttled`, 0x0 clean.
🔑 **START A PERCEPTION MEASUREMENT, THEN GO QUIET.** Commanding throughout starved the camera on 08-09 and produced two wrong answers. Short spikes vanish before `ps` — use **`tools/cpu_catcher.sh`**.
⚠️ **No rate/CPU number is trustworthy without `ps -eo pid,pcpu --sort=-pcpu` first.**
⚠️ **Replays need `ROS_DOMAIN_ID=42`; the LIVE stack is domain 0.** ⚠️ **`fps` is INERT in QGC.**
✅ **I can read/write PX4 params myself:** `python3 ~/ros2_ws/tools/set_param.py NAME [value]` (refuses while armed). **Don't ask the operator to read QGC.**

## [CURRENT STATE — 2026-08-10 23:30. ARMED SUCCESSFULLY 08-10 (S1) — the old "NEVER ARMED" is WITHDRAWN]
🔑🔑 **THERE IS NO VIO, AND NEVER WAS** — position = wheel ERPM + camera gyro dead reckoning ONLY; **RTAB-Map CONSUMES our TF odom** (`odom_frame_id: odom`) and adds `map→odom`, like AMCL. ⇒ M1/M2 need no VIO/localization. **Don't propose adding VIO.** → reference §6
🔑 **PX4 NEVER KNOWS WHERE IT IS, BY DESIGN** — bridge sends VELOCITY only. Measured 08-10: `xy_valid`/`v_xy_valid`/`heading_good_for_control` all **false**, `dead_reckoning true`. **Position lives in ROS, not the FC.**
🔴 **LOCALIZATION MAY NEVER COMMIT ITS FIXES** — `rtabmap_localization.yaml` records **4 good fixes in 12 min, NONE committed** (`map→odom` never moved). Accuracy is moot if the transform is static. **ONE RUN SETTLES IT; gates ALL M3 work — do it first.**
🔑 **"THE MAP IS GOOD" WAS TWO CLAIMS** — cloud operator-verified, **scale now tape-verified (line below)**; ⬜ the **2D grid a planner consumes has still NEVER been examined**. Check it before blaming a planner.
⚠️ **`autonomy_plan.md` HAS THE L0–L5 LADDER TWICE** (~L278, ~L429) — flagged in-file, unresolved, don't plan off either. **M-numbers are the ONLY mode numbering** (a 3rd scheme got `roadmap.md` archived).
🗺️ **GOOD ROOM MAP: `~/house_map_v4.db`** (bag `~/map_run_20260809_185011`, 0 drops) — shape/objects/floor correct, **walls SINGLE**, scale tape-verified. `rtabmap_localization.yaml` points at it.
⏭ **LOCALIZATION IS *NOT MEASURED*** — both 08-09 attempts invalid (one truncated window; one starved camera: colour collapsed 19.9→11.2 Hz mid-run and localization matches on COLOUR). **Don't quote a figure.** Retest = restart camera, verify colour 20-30 Hz, stop everything non-essential, **I stay quiet**, 5+ min watching the colour rate. → `autonav_reference.md` §13 (dated fitness) + §14 (method).
✅✅ **MAP SCALE VALIDATED BY TAPE 08-12 — `erpm_to_ms` 0.003900 IS RIGHT.** Y mapped 3.200 vs taped 3.1 (+3.2%); X mapped 2.450 vs taped 2.6 (−5.8%). 🔑 **Axes err in OPPOSITE directions ⇒ no global scale factor**; 19% compression would have needed Y at 3.83. X is LOCAL (a full-height object ~0.15 m proud of the wall; suspect = the glossy almirah, NOT confirmed). ⛔ **Do NOT 'correct' `erpm_to_ms` toward 0.004633/0.004463.** My 08-11 'contradiction' claim is **WITHDRAWN**. → autonav_reference §5/§13 · `indoor_mapping_slam` §13-16
⛔ **RECORD `/fmu/out/esc_status` IN FUTURE BAGS** — without it odometry can only be rescaled, not recomputed.
🔬 **DISPROVEN 08-09, don't re-propose:** map coverage · room light/auto-exposure (colour went UP in the dark — but tested in DAYLIGHT, so "a dark room hurts matching" is still open) · rover inside the 0.308 m depth min · camera-gyro rate-dependent scale (that was **my probe's arrival-time integration**) · `yaw_source: wheels` (skid-steer scrub ⇒ wheels cannot observe rotation; I proposed it twice).
🔴 **FC HEADING IS UNUSABLE — 2 UNTESTED SUSPECTS.** It is the **EKF's FUSED YAW, not the gyro** (3 IMUs, normal `CAL_GYRO*_ZOFF`). Suspects: the **single internal `bmm350`** (`EKF2_MAG_TYPE=1`, only yaw observer indoors) · **GPS** (`EKF2_GPS_CTRL=7`; ⚠️ **rate CONTRADICTED — 3.3 Hz recorded vs 0.0 Hz measured 08-10; resolve before ranking the suspects**). Test `EKF2_MAG_TYPE=5` / `EKF2_GPS_CTRL=0` separately, several runs each. **Rover-only; the DRONE shares this FC and needs both.** → autonav_reference §6
⏭ **QUEUED:** bridge `vehicle_angular_velocity` to DDS (commented out at `dds_topics.yaml:60`; 3 fused IMUs @398 Hz, would **decouple odometry from the camera**) — needs a FC flash, so pair it with other FC work. Camera roll **1.54°** still uncorrected (§4).

✅✅ **S1 (kill switch in AutoNav) PASSED** 07-22, re-confirmed 08-10 — `tools/s1_kill_test.py`. → `autonav_reference.md` §12/§13.
🔑🔑 **EVERY AUTONAV RUN NEEDS `rover-ekf-bridge` STARTED FIRST** (FLOOR only — limit-cycle hazard is wheels-UP; **stop it after**). Indoors it is the ONLY source of the local VELOCITY estimate AutoNav requires. Signature without it: **mode refused ARMED but accepted DISARMED**. → `setup_manual.md` §C10

✅ **REFLEX COLLISION-STOP: fail-open FIXED 08-10; ALL FOUR validations PASS** — A1 sensing · S2 stands acting · S2 floor wheels · standoff. The 08-10 wall contact was a RATCHET on a FRESH-BUT-EMPTY scan; the threshold was never wrong. Fix = perception-health gate BEFORE the clearance test (`min_valid_fraction`=0.35, inits 0). 🔑 **CUE: `collision-diag: BLOCK forward (BLIND)` + `scan_fresh=yes`.** → `autonav_reference.md` §8/§10/§12/§13 · `rover_autonav_collision_stop.md`
🔴🔴 **THE ROVER CONTACTED THE WALL 08-12 — I DROVE IT OUTSIDE ITS STOPPING ENVELOPE.** Commanded 0.25 ⇒ **ACTUAL ~0.9 m/s (3.6×)**; the reflex fired correctly at 0.322 m, but coast at that speed is ~0.30 m. **THE REFLEX WAS NOT AT FAULT — I sized the run off the COMMANDED speed while already holding measurement that commands aren't honoured.** ⛔ **GATE EVERY MOVING TEST ON MEASURED `/odom` SPEED, NEVER THE COMMAND** (`--max-measured-speed`). ⛔ **An odom travel backstop is USELESS at crawl** (odom under-reads 22% ⇒ 1.70 m limit ≈ 2.2 m real) — use `--min-bumper`.
🔴 **SPEED COMMANDS ARE NOT HONOURED — SAFETY-CRITICAL, not tuning.** 0.05→~0.11 · 0.06→0.146 · **0.25→~0.9**; floor ~0.1 m/s, ratio grows with command. **The vehicle cannot be commanded to stay inside the speed band its stopping distance requires.** ⛔ **T2 at crawl only; NO speed increase until this is fixed** — every standoff guarantee is conditional on ~0.1 m/s.
✅ **STANDOFF n=3 TAPE-CONFIRMED (0.345 m) — BUT SPEED-BOUND** (true ~0.11 m/s, FALSE ~0.9 m/s ⇒ 0.020 m); **set by the COAST, not the threshold**. ✅ **`/scan` scale 0.9845 (−1.55%), offset ≈0** ⇒ **`front_overhang` 0.337 CONFIRMED to 2 mm**; `/scan` reads SHORT ⇒ reflex fires EARLY (safe); apply 1.55% when comparing `/scan` travel to another ruler. → autonav_reference §5/§12/§13
🔴 **`/odom` SCALE IS SPEED-DEPENDENT — the answer to the 17%.** +5% at ~0.9 m/s · **−22% at ~0.11 m/s** (twice); slip rising with speed. Fast figure ≈ configured 0.003900, which is why the map taped true. ⛔ **`erpm_to_ms` CANNOT be fixed with one new number — any correction must be speed-aware.** ⚠️ Crawl figure EXCEEDS the geometric 0.004633, which slip cannot do — **unexplained, don't fit a story.** → autonav_reference §5/§13
🔴🔴 **TRAPS THAT WILL BITE AGAIN — the reason to open the file:** a **DISARMED external mode reads as SELECTED but is NOT RUNNING** (needs armed OR `activate_even_while_disarmed`; a silent setpoint topic ≡ 'the reflex held' — a FALSE-PASS GENERATOR, fixed for AutoNav) · **never quote 'last scan → wheels stopped' as reflex latency** (DECIDE + a MECHANICAL COAST, opposite fixes) · **`/scan` does NOT come back after a scan-killing test and `is-active` LIES** ⇒ restart + VERIFY by subscribing. ⛔ **Never read a quiet topic as evidence — prove a NON-ZERO baseline first.** → autonav_reference §10 · setup_manual §C3
❓ **Open: WHY the wall vanished from the scan** (3 candidates) — the gate holds regardless. → autonav_reference §10
⏭ **S2b** (kill `rover-camera` ⇒ loses `/scan` AND the heading gyro together) is separate and harsher than S2 floor.

## [TODOS] → memory/todos.md
**⏭ NEXT (2026-08-13): (a) 🔴 THE SPEED-COMMAND FAULT — it gates everything** (0.25 cmd → ~0.9 m/s; suspect PX4 `RO_*` speed scaling; `tools/set_param.py` reads/writes params) · (b) T2 straight goal **at crawl, gated on measured speed** · (c) does localization ever COMMIT `map→odom`? (cheap, no vehicle, gates all M3) · (d) S2b. ✅ DONE: A1 · S2 stands · S2 floor · standoff(n=3, taped) · map scale · `/scan` calib. ✅ CLOSED: #20 yaw · #23 watchdog · #25 retry storm · #26 depth-glitch · **#26b** · #28 camera 30 fps. #21 gyro-yaw open.
1. Relay NTP · 2. 🔴 **NO onboard Wi-Fi fallback — recovery is WFB → relay:2222 ONLY** · 14. 🔶 **PAT: SSH done 08-09; STILL MUST REVOKE THE TOKEN + check `codex-relay`**
3+4+24. ❌ **DELETED, don't re-propose:** GS `rx_ring_size`, GS TX power (maxed), trim PX4 MAVLink rates.
5+7+8+9+10. Antenna tracker HW · /scan → Nav2 · #17 delete `camera_sw_node_obsolute.py` (+`rov_collision_stop`, `collision_manual_mode`) · Multicam Phase D (#25 **still keys `/dev/video0`** — close via Phase D)
27. ⏭ Reflex `/scan_3d` is a PARAM (`collision.scan_topic`), default still `/scan`. **Needs ONE low/overhanging object `/scan` misses before flipping.**
**[OUTDOOR] O1-O5:** STL-19 · DroneCAN GPS · lidar SLAM · GPS Nav2 · outdoor safety. **WFB parked: #22 = HARDWARE, reseat drone NIC-A ant0.**
⚠️ **`align_mode:=HW` untested** — would kill the depth-glitch class and save 71%/core. ⚠️ **Orbbec clone is GITIGNORED — a re-clone RESTORES the #26 bug** (patch: `codex-work 16665f5`).

## [FPV CAMERAS] — not in the manuals (autonomy camera is in setup_manual C2)
Both FPV-capable, port 6-2, **swap from QGC only**: **See3CAM_CU135** `usbcam-2560c1d1-241D8306-i00` · **LG Smart Cam** `usbcam-30c9009d-01.00.00-i00` = **the current FPV cam**.
⛔ **Never key a camera by `/dev/videoN` or by-id** — only `usbcam-<vidpid>-<serial>-i<iface>`; Orbbec nodes vanish with `rover-camera` and the Pi's `rpivid`/`pispbe-*` renumber them.
**⚡ FPV video costs `/scan` 28.4→22.3 Hz.** ⛔ **Do NOT modify the ffmpeg command line** (vetoed). `vision_streaming` STOPPED since 08-08. Detail: `vision_streaming.md`.
## [WFB_NG] → reference_wfb_ng.md (**PARKED — only action left is HW: reseat drone NIC-A ant0**)
ch161 | drone-wfb@10.5.5.87 ↔ gs-wfb@10.5.5.77 | keys `/etc/{drone,gs}.key` | **Drone TX is flawless — when video breaks WFB's input queue is EMPTY, SUSPECT THE SOURCE.** Stats: TCP `127.0.0.1:8102`/8103 JSON, **not** `wfb-cli`.
⚠️ **METHOD (cost a week):** compare payload `tx.incoming`→`rx.out` across 8102/8103. **Never `rx.all`** (double-counts 4 antennas). **Never infer radio health from MAVLink rates at `tcp:5760`.** Detail: `reference_wfb_ng.md`.
## [RELAY_STATION] → `relay2_relaystn`
`vind-rly` | RPi5 | `ssh vind-admin@10.5.5.77` (**sudo NEEDS A PASSWORD ⇒ journalctl of other units = "No entries"**) | `~/codex-relay` | tunnel 2222→drone :22 | **NO RTC** | svcs: `wifibroadcast@gs`, `mavlink.router`, `ssh-tunnel-to-companion`, `relay_files_sync.timer` | **Wi-Fi Direct P2P-GO `p2p-wlan0-0`, SSID `vind_rely`, ch149, 10.5.6.101/24 → QGC 10.5.6.50**
## [REPOS / GCS]
`codex-work`: `~/codex-work` → Companion_Computer_Pxlabs, branch **master** (origin/main stale) | `codex-relay`: on vind-rly → Relay_Station_Pxlabs | `ros2_ws`: `~/ros2_ws`, **main**, SSH ✅
GCS: G-Control.exe → pxlabs_cli.exe → SSH relay:2222 → companion:22 | QGC: ArvinVeiyon/PXLABS_qgroundcontrol @ PXLABS-integration
🔴 **08-09: all remotes on SSH, no repo here carries a token — BUT THE PAT IS STILL LIVE ON GITHUB, REVOKE IT** (github.com/settings/tokens). Switching the remote stopped it being USED, not rotated. ⏭ `~/codex-relay` on vind-rly never checked for the same pattern.
## [SERVICE / TROUBLESHOOTING QUICK INDEX] → full detail in setup_manual C, D2, E5
**AIDE timer DISABLED** (~3.5 h/day of a core). **`tfmini` DISABLED — ⚠️ MUST `systemctl enable --now tfmini` for the DRONE.** **`rover-ekf-bridge` DISABLED on purpose** (wheels-up limit-cycle hazard); it already uses `LocalPositionMeasurementInterface`, velocity-only ⇒ **the Q1 hook is half-built.**
no_MAVLink → `ttyAMA0` + the PX4 MAVLink instance | no_DDS → `microxrce-agent`, `ttyAMA4` | WFB_down → `wifibroadcast@drone`, `wlx*`, `/etc/drone.key` | **no_video → CPU-starvation latch FIRST (a restart does NOT clear it)** | no_scan/no_cloud → **half-dead camera restart first** | localization degrading → **measure the colour rate, then restart the camera**
