# Vind-Roz Platform Memory
> Auto-loaded each session; also the Phi-3 offline prompt. Live: `~/.claude/projects/-home-roz/memory/` | Backup: `~/codex-work/memory/` → ArvinVeiyon/Companion_Computer_Pxlabs
> ⚠️ **BACKUP IS MANUAL:** `cp -p .../memory/*.md ~/codex-work/memory/` + push. **Never `rsync --delete`** (the mirror is a UNION of 2 scopes).
> ⚠️ **INDEX, NOT A STORE. One line per entry — leave a POINTER, not the fact.** 🔑 Judge by that, not by line count. 🔴🔴 **CAP = 24.4 KB AND IT TRUNCATES THE TAIL SILENTLY — no error, no marker** (verified 09-18 in the claude binary). **17.1 KB is the recompaction TARGET, never the ceiling** — ⛔ don't read the hook's nag as the limit, and ⛔ don't obey it mid-task. **KEEP ≥4 KB HEADROOM (≤20.4 KB).** ⚠️ measure with **`wc -c`**: python `len()` counts CHARACTERS and reads ~6% low on this emoji-heavy file.

## 📘 THE TWO MANUALS — READ BEFORE ASKING THE OPERATOR OR DERIVING ANYTHING
📐 **`~/ros2_ws/docs/autonav_reference.md`** — *what is TRUE and why.* §5 constants · §10 faults · §11 rules · §12 ladder · §13 dated fitness. **A bare `§N` below = this file.**
📕 **`setup_manual.md`** — *what to DO.* §C nodes (§C10: an external mode's needs come from the SETPOINT TYPE) · §D6 arming · §E2b map verification · §A7 params (`RO_*` rover-only vs `EKF2_*` **SHARED WITH THE DRONE**). ⚠️ Part A mostly UNVERIFIED.
📗 **`docs/README.md` = THE DOC INDEX, open it.** `autonomy_plan.md` M0-M4 = the ONLY mode numbering · ⚠️ `ros2_architecture.md` STALE · 🗄 `roadmap.md` archived. 🔑 **Count of sources ≠ weight of evidence.**
🔑 **Arming AutoNav: external mode, NO RC switch slot** ⇒ RC arm lands in **Manual**; arm there, then companion sends `DO_SET_MODE main=4 sub=11`. ⛔ **NEVER arm AutoNav on stands with the EKF bridge running** (self-sustaining limit cycle). → setup_manual §D6

## [MEMORY_FILES]
**feedback (RULES) — 🔴 OPEN THE FILE, THE ONE-LINER IS NOT THE RULE:** `ask_before_param_change` **(⛔ NEVER WRITE A VEHICLE PARAM WITHOUT AN EXPLICIT YES; reading is free)** · `verify_after_editing` · `notify_before_recording` · `describe_before_executing` **(⛔ "armed" is NOT "go")** · `answer_short` · `explain_in_prose` · `defer_memory_updates` · `debug_before_documenting` · `scope_px4_params_by_control_flags` · `test_before_concluding` · `log_the_discriminator` · `independent_rulers` · `check_docs_before_measuring` · `eliminate_hypothesis_whole_family` · `crash_recovery_checkpoint` · `use_dds_not_mavlink` · `camera_qgc_only` · `dkms_arch` · `wlan0_persistent_name`
**project — ACTIVE:** `rover_autonav` · `esc_config_audit` (🔴🔴 **HARD NEUTRAL BRAKE UNDIAGNOSED, needs FLOOR** · 🔴 RR's SUSPECT hall table is the operator's call — ⛔ don't "fix" it · 🔑🔑 **CRAWL CATCH = THE SPEED LOOP**; ⚠️ hand-spin drag tests are MEANINGLESS POWERED ON) · `indoor_mapping_slam` (§13-17) · `perception_3d_costmap` · `autonomy_plan_reframe` · `vesc_can_flashing` (🔴 `60_mk5.bin` OVER DRONECAN = BRICK, USB ONLY · 🅿️ CAN integration DROPPED, ⛔ don't re-debug the MCP2515)
**reference:** `px4_rover_control_scope` (🔴 WHICH `RO_*` REACHES WHICH MODE) · `vesc_tool_cli` (🔑🔑 **VESC Tool 7.01 BUILT HERE, HEADLESS** — ⛔ NEVER cross-write a wheel's mcconf) · `esc_telemetry` · `services` · `this_machine` · `wfb_ng` · `px4_vio_collision` (🔑 **PX4 collision prevention is MULTICOPTER-ONLY**) · `gcs_companion_interface` · `uart_map` · `wfb_cfg_apply` · `wfb_rlyctl` · `known_fixes_archive` · `ros2_nodes` · `ros2_topics` · `rover_odometry`
**project — other:** `fc_hardfaults` · `rc_ch10_reboots_companion` · `l4_gemini_nav2_prereqs` · `vision_multicam_upgrade` · `l2_floortest_wheel0_reversed` · `wfb_undervoltage_dead_nic` · `external_wifi_uplink` · `gcs_link_degraded` · `relay_ntp_setup` · `relay2_relaystn` · `companion_network_degraded` · `boxb_pcie_usb` · `codexrelay_divergence` · `codexwork_token_in_remote` (🔴 PAT) · `codexwork_branches` · `rc_control_camera_retry_storm` (🔑 runaway `vision_config_manager` — burns a core) · `ros2ws_tag_cleanup` · `mavlink_shell_session_exhaustion`
🔑 **A file this index does not name does not exist at session start** — 10 of the above were unreachable until 09-19.

## [WORKING ON THIS MACHINE]
🗂️ **`$HOME` layout, the moved map, and the 85%-full disk → `this_machine`.** ⛔ `ros2_ws` + `codex-work` did NOT move.
⚠️ **sudo NEEDS the password:** `printf '1987\n' | sudo -S …`
⚠️ **`pkill -f <pat>` MATCHES MY OWN SHELL** (self-kill, exit 144). Use `pgrep -x` / `pgrep -f "[p]attern"` then `kill`.
🔴 **I AM A MAJOR CPU CONSUMER:** `claude` runs 55-86% of a core. Power check `vcgencmd get_throttled` (0x0 clean).
✅ **I can read/write PX4 params myself** — `tools/set_param.py`. Don't ask the operator to read QGC.
⚠️ **ANYTHING THAT MAY RUN >30 s MUST SAY SO OR GO TO BACKGROUND** — a silent terminal reads as a dead box from the operator's chair. ⛔ never regex-sweep `~/.claude/projects/-home-roz/*.jsonl` (54 MB; one such grep took 2m11s).
📄 **Measurement hygiene · `cpu_catcher.sh` → `this_machine`.** 🔴 **EVERY NIC NAME IN THE DOCS IS STALE** (`lsusb` first). ⚠️ Boot clock WRONG until NTP · replays `ROS_DOMAIN_ID=42`, live 0.

## [CURRENT STATE]
🔑 **NO VIO IS *RUNNING*** — position = wheel ERPM + camera gyro dead reckoning; RTAB-Map CONSUMES our TF odom. 🔴 **NOT a capability gap — `rgbd_odometry`/`icp_odometry` ARE installed and have run here; the blocker is CPU + the plate-in-frame problem.** ⛔ Don't say "we can't do VIO" — say what it costs. → §6
🔑 **PX4 NEVER KNOWS WHERE IT IS, BY DESIGN** — the bridge sends VELOCITY only. ⚠️ Always state the bridge's state when quoting `v_xy_valid`.
🔴 **LOCALIZATION DEAD — 0 accepted of 20 on the map's OWN bag ⇒ pipeline fault, fails at GEOMETRY not appearance.** ⛔ never lower `Vis/MinInliers` · ⛔ don't re-map the house · ❌ the DB-param-mismatch lead is DEAD. → `indoor_mapping_slam` §17
✅ **2D GRID IS FINE** — ⛔ don't "fix" `MaxGroundHeight` (keep 0.10) or `NormalsSegmentation`.
🗺️ **GOOD ROOM MAP `rover_data/maps/house_map_v4.db`** (bag `map_run_20260809_185011`) — walls single, scale tape-verified.
🔴 **THE ROOM IS THE CONSTRAINT (~2 m² open floor)** ⇒ ⛔ **T2 CANNOT RUN IN *THIS ROOM*** — ✅✅ it PASSED IN THE CORRIDOR 09-16. ⚠️ footprint **0.73 × 0.450 m**; **0.560 was a SALES QUOTATION** copied by four docs. ⛔ `wheelbase` 0.430 is NOT the track; `RD_WHEEL_TRACK` 0.31 is CORRECT.
⛔⛔ **THE CAMERA IS MOUNTED CORRECTLY — STOP RAISING "CAMERA ROTATED"; the operator has been right every time.** ✅✅ **G0 CLOSED 09-11**, verified at a wall ⇒ ⛔ **don't re-derive the mount numbers.** → `project_rover_autonav` 09-11
✅ **"uXRCE UART REBOOTS THE COMPANION" = RC CH10, NOT A FAULT** (2014=reboot · 1514=shutdown · 1011=safe). 🔑 **CHECK CH10 BEFORE debugging any companion restart** — but 🔑 **CH10 AND EVERY CLEAN REBOOT LEAVE A SYSTEMD SHUTDOWN SEQUENCE, so a journal that just STOPS EXONERATES IT.** → `rc_ch10_reboots_companion`
🔴 **FC HEADING IS UNUSABLE — it is the EKF's FUSED YAW, not the gyro.** 2 untested suspects: `bmm350` (`EKF2_MAG_TYPE=1`) · GPS (`EKF2_GPS_CTRL=7`). ⚠️ the DRONE shares this FC and needs both. → §6
🔬 **DISPROVEN 08-09, don't re-propose:** map coverage · light/exposure · rover inside the 0.308 m depth min · camera-gyro rate-dependent scale · `yaw_source: wheels`.
✅ **LADDERS: M0-M4=goal · L0-L5=capability · R1-R7=reqs.** ⏭⏭ THE PLAN = `todos.md` §REQUIREMENTS REALIGNMENT, gates G0-G7.
⏭ **QUEUED:** bridge `vehicle_angular_velocity` to DDS (`dds_topics.yaml:60`) — decouples odometry from the camera; needs an FC flash.

## [AUTONAV — ARMED STATE]
✅✅ **ARMED AUTONAV REPEATABLE:** reboot FC → restart bridge + autonav-mode TOGETHER (registration needs DISARMED) → verify 3 gates → arm ch5. 📏 budget ~45-60 min per bridge restart.
🔴🔴🔴 **ARMED AUTONAV IS REFUSED BY `eph` vs `COM_POS_FS_EPH`=5 m, NOT by the mode/bridge/registration.** PX4 relaxes requirements DISARMED, enforces them ARMED. ⛔ **NEVER call S1/AutoNav "works" or "broken" without the `eph` at that moment.** ⏭ durable fix = LOCALIZATION. ⚠️ raising the threshold disables a real failsafe — operator call only.
⛔⛔ **PX4 WILL NOT REGISTER AN EXTERNAL MODE WHILE ARMED.** DISARM → restart the node → confirm registration → ARM. 🔴 **AN FC REBOOT WIPES IT AND THE NODE DOES NOT NOTICE.** 🔑 proof = the arming-check handshake, NOT `is-active`.
🔑🔑 **EVERY AUTONAV RUN NEEDS `rover-ekf-bridge` FIRST** (FLOOR only; stop it after). Refused ARMED without it. ⚠️ armed+idle WITH the bridge = delayed motor KICKS (limit cycle) — disarm between runs. → setup_manual §C10
⛔⛔ **THE KILL SWITCH IS `ch12`, NOT ch8** (`RC_MAP_KILL_SW`=12 · `ARM_SW`=5 · `FLTMODE`=6 · `KILLSWITCH_TH` 0.75; nothing is on ch8). ✅ fixed in all 6 moving tools. ⚠️ **PX4 KILL ≠ DISARM** — settle what S1 means before re-running.

## [MOTION / SAFETY]
✅ **REFLEX COLLISION-STOP: fail-open FIXED 08-10, 4/4** — perception-health gate BEFORE the clearance test (`min_valid_fraction` 0.35). 🔑 CUE: `BLOCK forward (BLIND)`. 🔑 **A high NaN count is NOT blindness** — the corridor filter drops everything outside 0.275 m by design. → §8
✅✅ **SPEED COMMANDS ARE HONOURED — ALL FOUR IN RPM MODE.** `RO_MAX_THR_SPEED` 0.60→**4.93**. ⛔⛔ **EVERY TORQUE-MODE RULE IS HISTORY** ("throttle is TORQUE" · "don't calibrate `RO_MAX_THR_SPEED`" · "overspeed is an INTEGRAL" · "DURATION is the control") — **do not quote them.**
✅✅ **BELOW `s_pid_min_erpm` THE ESC APPLIES A DUTY-0 SHORT BRAKE** (`CONTROL_MODE_SPEED` only) ⇒ **it FINISHES the stop, ⛔ NEVER 0.** 🔑 ramp is SYMMETRIC. ⛔ "free coast below min_erpm" WAS WRONG. 🔴 **REGEN IS CUT ON A CHARGED PACK** ⇒ spin-down is SCRUB, not regen; ⛔ don't raise the cut-in. **Floor numbers → `esc_config_audit`.**
🔴🔴 **THE REFLEX ONLY ZEROES THE SETPOINT — a real brake in RPM mode, but ONLY at ramp 20000.** ⛔⛔ **NEVER CUT `s_pid_ramp_erpms_s`** — it slews the EMERGENCY stop to comfort rate, AND on a differential drive throttles YAW ONSET ⇒ CONTROL AUTHORITY, not comfort. ⏭ durable fix = the reflex COMMANDS a brake (VESC **firmware** change). ⛔⛔ **DO NOT CHASE THE HARD NEUTRAL BRAKE WITH `l_current_min` — no candidate binds; the STAND CANNOT REPRODUCE IT.** ⚠️ **both brake loggers lie**, and 🔑🔑 **WHEEL RPM CANNOT MEASURE A STOP AT ALL — use tape or `/scan`.** ⛔ USB only, 1 session/ESC; hub `1-1.2` reset, ⛔ **NEVER `1-1`** (Realtek NICs + WFB ride on it). → `esc_config_audit` §THE BRAKE PATH
🔑 **A POWERED-OFF ESC IS INDISTINGUISHABLE FROM A DEAD BUS** (`online_flags` clear + `timestamp==0`). ⛔ **ASK "are they powered?" BEFORE diagnosing CAN.** 🔑 1 driven + 3 off = SLOW ROTATION, not a low cap.
🔑 **`esc_count` IS 6.** ⛔ QGC ESC health PERMANENTLY LIT ⇒ read `esc_online_flags` over DDS. ⛔ **NEVER index `esc[]` by position — key on `esc_address`.**
🔑🔑 **"ESC 1/2" IN A PX4 MESSAGE NAMES A SIDE, NOT A WHEEL — and 3/4 CAN NEVER APPEAR.** `EC_FUNC1/3`=101=Motor1=**RIGHT** (addr 10+12) · `FUNC2/4`=102=Motor2=**LEFT** (addr 11+13) — correct for a diff drive, ⛔ don't "fix" it. ⇒ **PX4 CANNOT SAY WHICH WHEEL DIED, only which side.** → `esc_telemetry`
✅✅ **ODOMETRY: `erpm_to_ms`=0.003900 ⛔ NEVER RE-OPEN THE SCALE.** 🔑🔑 **THE ERROR IS SPEED-DEPENDENT** (dropout under-reads slow, slip over-reads fast, crossover ~0.25 m/s) ⇒ the old "8.1% over" and "24% under" are two ends of ONE curve. 🔑 **QUOTE EVERY ODOM DISTANCE WITH ITS SPEED.** 🔴 `si_motor_poles` is a LINKED PAIR — "fixing" poles SILENTLY HALVES `/odom`. **ADDR 10=RF 11=FL 12=RR 13=RL** ⚠️ **L/R is PROVEN, FRONT/REAR is NOT — spin one wheel before any per-corner claim.** → `rover_odometry`
🔑 **`esc_current` = the LOADED/UNLOADED ruler; NEGATIVE current proves the brake engaged.** ✅✅ **09-19 RETRACTION: "`/odom` DRIFTS AT STANDSTILL" IS FALSE** (0 m over 10 s disarmed, once `deadband_erpm` went 40→5). ⚠️ **unchecked ARMED.**
🔴 **TRAPS THAT WILL BITE AGAIN:** a DISARMED external mode reads SELECTED but is NOT RUNNING · never quote "last scan → wheels stopped" as reflex latency · `/scan` does NOT come back after a scan-killing test · 🔴🔴 **TURN/360 STICK IS ch1 → `.roll`; `RC_MAP_YAW`=4 IS A RED HERRING, ch4 IS EMPTY** · 🔴 **it is `/fmu/out/vehicle_status_v1`** — the old name gives NO arming data, and a `None` default reads "disarmed" while wheels spin. ⛔ **Never read a quiet topic as evidence — prove a NON-ZERO baseline first.** 🔴 **`/scan` DOES NOT TRACK BEYOND ~3 m** — **park ~2.5 m out.** → §10
🔴 **08-16 I KILLED THE GCS MAVLINK LINK — ONE `mavlink_shell.py` PER FC BOOT, NEVER RETRY AN EMPTY RESULT.** Recovery = reboot over DDS. → `mavlink_shell_session_exhaustion`

## [TODOS] → memory/todos.md
⏭⏭ **FOCUS: AUTONAV → M2, CAMERA-ONLY. OPEN `todos.md` START HERE + `project_rover_autonav` 09-19/09-20 — THE DETAIL IS THERE, NOT HERE.** ✅✅ **09-19: NAV2 DROVE AN ARMED ROVER; T3's AVOIDANCE REPRODUCED.** **ARMED RUNS USE `nav2_forward_flat.yaml`**; ⛔⛔ never `nav2_forward.yaml` (voxel = MAX-RATE SPIN). 🔑🔑 **ONE NUMBER LEFT FOR T3: `ObstacleFootprint.scale` — TRY 10-15, THEN 3 RUNS UNCHANGED.** 🔑 **CLEAR BOTH COSTMAPS BEFORE EVERY RUN.** 🔑 **SAY WHICH RULER** — `t3r.py` RAW vs preflight BUMPER, 0.337 m apart. ⛔ odom heading is a RED HERRING; the SCENE matters. ⛔ **a goal inside obstacle+inflation fails in the PLANNER and looks like a controller bug.** ⛔⛔ **DWB READS `sim_time` AND CRITIC `scale` AT INITIALISE — a live `param set` does NOTHING. YAML + RESTART.** ⚠️ spin-guard critics still OUT. 🅿️ **RPP parked.** 🔴🔴 **SIGN BUG FIXED 09-19** (`/cmd_vel angular.z`, ROS FLU vs PX4 FRD). 🔴 **RR DEMAGNETISED — REPLACE FIRST**; ⛔⛔ **NO PIVOTS FROM REST.** 🔴 ESCs set 350 W on 250 W motors. 🔴 **YAW UNRESOLVED** — `CORR`=7.4, ⛔⛔ `RO_YAW_RATE_I` STAYS 0, ⛔ `/odom` yaw unusable at speed. 🔑 **ch12 KILL PRESSED ARMED 09-19, rover STOPPED** (⚠️ not a formal pass). 🔑 **OPS:** ch5 **CYCLED down-up** · `eph` on **`vehicle_local_position_v1`** · **reboot FC + bridge TOGETHER** · reflex corridor **|y|≤0.275 m** · ⛔ **LIDAR NOT REQUIRED.**
✅ **FC HARDFAULTS CLOSED 08-29 — ⛔ DON'T REOPEN.** 🔴 **ONLY LIVE HAZARD: the fw is OUR OWN BUILD ⇒ ANY reflash from upstream or `~/apps/fc_firmware` BRINGS THEM BACK — verify by GIT HASH, not `flight_sw_version`.** `a52c38b07d`, tag `pxlabs-v1.17.0-r2-Beta`. → `fc_hardfaults`
🔧 **PX4 PARAMS — READ THE FC, NEVER TRUST A SNAPSHOT.** ⛔⛔ **NEVER WRITE ONE WITHOUT AN EXPLICIT YES.** 🔴 **`RO_DECEL_LIM`=5 since 09-14, `RO_ACCEL_LIM`=−1** (the "keep both at −1" rule is SUPERSEDED); they slew the MANUAL stick **and the collision reflex**. ⚠️ re-verify reflex standoff after any decel change. **→ `px4_rover_control_scope` for the ramp arithmetic, the scope map, AND the write/read tooling rules** (`NAV_RCL_ACT`=1 ⇒ RC loss will NOT disarm).
🔑 **BOOT-CLOCK TRAP — `/proc/uptime` BEFORE READING ANY DURATION OFF A TIMESTAMP.** ⚠️ **a GAP in a timestamped log is not a fault either — grep `timesyncd` for a clock STEP first.** → `this_machine`
**Numbered backlog → `todos.md`.** ✅ #22/#25/#26/#27 live there; **#23/#24 never used.** 🔑 **numbers are the contract between the two files — never cite one that doesn't exist.** ⛔ **`5. Antenna tracker HW` was DROPPED 09-19. Do not resurrect it.**
🔴 **NO Wi-Fi fallback — remote = WFB → relay:2222**; ⚠️ eth0 wired `ssh roz@10.10.10.10` UNTESTED → setup_manual §E5b. **[OUTDOOR] O1-O5:** STL-19 · DroneCAN GPS · lidar SLAM · GPS Nav2 · outdoor safety. ⚠️ **`align_mode:=HW` untested** — kills the depth-glitch class, saves 71%/core.

## [FPV CAMERAS] → `docs/vision_streaming.md` (autonomy camera → setup_manual C2)
Both FPV-capable, port 6-2, **swap from QGC only**; LG Smart Cam current. ⛔ **Never key a camera by `/dev/videoN`** — only `usbcam-<vidpid>-<serial>-i<iface>`. ⚡ FPV video costs `/scan` 28.4→22.3 Hz. ⛔ Do NOT modify the ffmpeg line (vetoed). ⚠️ `vision_streaming` RUNNING ≠ streaming.
## [WFB_NG] → `wfb_ng` (**PARKED — only action left is HW: reseat drone NIC-A ant0 = #22**)
**Drone TX is flawless — when video breaks WFB's input queue is EMPTY: SUSPECT THE SOURCE.** ⚠️ the measurement METHOD cost a week — read the file first.
## [RELAY_STATION] → `relay2_relaystn`
`vind-rly` RPi5 | `ssh vind-admin@10.5.5.77` (sudo needs a password) | tunnel 2222→drone :22 | NO RTC. ✅ 08-28 CLOSED: relay MAVLink + cluster-wipe loop. ⛔ no Pi Zero 2 W relay · never `wfb-rlyctl use-cluster`, use `disable` (`mask` FAILS here). 🔑 ping to the QGC laptop is a FALSE NEGATIVE (use `ip neigh`) · a repo pull DEPLOYS NOTHING.
## [REPOS / GCS]
📗 **`codex-work/README.md` = THE COMPANION DOC INDEX — open it before hunting for a doc.** ⚠️ distinct from `ros2_ws/docs/README.md` (autonomy side).
`codex-work` → Companion_Computer_Pxlabs, branch **master** (origin/main stale); **docs live at the ROOT — rover/DroneCAN docs belong HERE, not `ros2_ws`** | `codex-relay` on vind-rly → Relay_Station_Pxlabs | `ros2_ws` → ArvinVeiyon/ros2_ws, on `main`.
GCS: G-Control.exe → pxlabs_cli.exe → SSH relay:2222 → companion:22 | QGC: ArvinVeiyon/PXLABS_qgroundcontrol @ PXLABS-integration
🔴🔴 **2 LIVE PATs STILL UNREVOKED (08-20): `~/git_key` + a CLASSIC scope-`repo` token in `~/.claude/history.jsonl`.** ⛔ **OPERATOR MUST REVOKE IN A BROWSER.** ✅ Revoking breaks nothing (all remotes SSH). 🔑 **NEVER PASTE A SECRET INTO A PROMPT.** → `codexwork_token_in_remote`
## [SERVICE / TROUBLESHOOTING QUICK INDEX] → detail in setup_manual C, D2, E5
**Service state → `reference_services`** (AIDE timer OFF · ⚠️ `tfmini` DISABLED — MUST enable for the DRONE · `rover-ekf-bridge` **and `rover-nav2`** DISABLED on purpose — both MOVE the rover, manual start per run; 🔑 **restarting `rover-nav2` IS the costmap clear**; 15-node chain → `setup_manual` §C6b). 🔴🔴 **`active` PROVES NOTHING — twice, all units read `active` while depth/`/scan`/`/odom` were 0.0 Hz. MEASURE RATES, NEVER `is-active`.**
🔑 **THE ros2 CLI IS ITSELF AN UNRELIABLE RULER** — `node list` ghosts for minutes; `echo`/`hz` can return NOTHING on a live topic. Prove dead with `pgrep`; measure with a direct rclpy subscriber. → `this_machine`
✅ **IS THE BOX ALIVE? `tail -2 /var/log/ext5v/ext5v.csv`** — stamped every 2 s, independent of tmux/Claude ⇒ a fresh row means ALIVE. Use it before ever cutting power on a silent terminal.
no_MAVLink → `ttyAMA0` + the PX4 MAVLink instance (**DDS fine ⇒ FC alive, reboot over DDS**) | no_DDS → `microxrce-agent`, `ttyAMA4` | WFB_down → `wifibroadcast@drone` | **no_video → CPU-starvation latch FIRST** | no_scan/no_cloud → **restart the camera THEN `rover-scan`/`-scan-3d`/`-odometry`, in that order — 0 Hz forever otherwise** (twice: the colour stream died ⇒ depth registration had no align target ⇒ every frame dropped)
