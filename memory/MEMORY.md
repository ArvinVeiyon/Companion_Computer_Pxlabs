# Vind-Roz Platform Memory
> Auto-loaded each session; also the Phi-3 offline system prompt. Live: `~/.claude/projects/-home-roz/memory/` | Backup: `~/codex-work/memory/` → GitHub ArvinVeiyon/Companion_Computer_Pxlabs
> ⚠️ **BACKUP IS MANUAL:** `cp -p ~/.claude/projects/-home-roz/memory/*.md ~/codex-work/memory/` + git push. **Never `rsync --delete`** — the mirror is a UNION of two scopes; the 2nd (`-home-roz-codex-work`) is NOT auto-loaded, check it after any codex-work session.
> ⚠️ **KEEP UNDER 125 LINES / 17 KB.** One line per entry. **Technical detail belongs in the two manuals below — put a POINTER here, not the fact.**

## 📘 THE TWO MANUALS — READ THESE BEFORE ASKING THE OPERATOR OR DERIVING ANYTHING
📐 **`~/ros2_ws/docs/autonav_reference.md`** — *what is TRUE and why.* 13 cited sections: §1 goal · §2 the four questions · §3 platform+CPU budget · §4 geometry/frames · **§5 calibrated constants WITH THEIR METHODS** · §6 odometry+heading · §7 motion envelope (yaw deadband) · §8 perception · §9 mapping/localization · §10 known faults · **§11 inviolable rules** · §12 test ladder+status · §13 method notes.
📕 **`~/ros2_ws/docs/setup_manual.md`** — *what to DO.* A FC firmware/calibration/params · B companion build · **C NODE INVENTORY (every service's real ExecStart, every node's declared params)** · D bring-up+verification (**§D6 arming into AutoNav**) · E routine ops (bag record, replay-map, **§E2b MANUAL MAP VERIFICATION — `tools/map_review.py` + the 5 operator questions, Q1 "any wall drawn twice" decides it**, localize, measure, recovery) · **A7 = THE PX4 PARAMETER CHANGELOG (every change + the test that justified it; `RO_*` rover-only vs `EKF2_*` SHARED WITH THE DRONE; `EKF2_*` are as-FOUND, not as-changed)** · **F stubs still to write: map creation, QGC map integration, mission execution, Nav2 config, outdoor, drone revert.**
🔑 **Read §5 before trusting any constant · §11 before designing any behaviour · setup_manual C before touching a node · E before running a procedure.** ⚠️ setup_manual Part A (FC calibration) is mostly UNVERIFIED-ON-THIS-VEHICLE (marked 📋) — correct it the next time a calibration is actually run.
📗 **`docs/README.md` = the doc index** (13 files → 2 entry points + 6 deep + `archive/`). Deeper: `rover_geometry.md` (every dimension) · `rover_yaw_response.md` (yaw derivation) · `vision_streaming.md` (FPV faults) · `autonomy_plan.md` (modes M0-M4) · `rover_autonav_requirements.md` · `indoor_mapping_plan.md`. ⚠️ `ros2_architecture.md` is STALE (setup_manual C supersedes it). 🗄 archived: `roadmap.md` (L-numbers clash with autonomy_plan = hazard) · `yaw_tuning_session.md` · `third_party.md` (folded into setup_manual §B2).
🔴🔴 **UNRESOLVED SAFETY CONFLICT — DO NOT ASSUME EITHER WAY.** `rover_autonav_collision_stop.md` (07-23) says the **ch8 kill is "proven to work armed inside AutoNav"**; memory + `autonomy_plan.md` + `autonav_reference.md` §12 all say **S1 is UNTESTED**. Both cannot be true. **Resolve by RUNNING THE TEST** (floor, 0.15 m/s in AutoNav, hit ch8), then correct whichever record is wrong. → setup_manual §D6
🔑 **Arming AutoNav: it is an external mode with NO RC switch slot** ⇒ RC arm always lands in **Manual**. Path = arm in Manual (throttle neutral) → companion sends `DO_SET_MODE main=4 sub=11`. ⛔ **never arm AutoNav on stands with the EKF bridge running** (self-sustaining limit cycle; only a disarm stops it). → setup_manual §D6

## [MEMORY_FILES]
**feedback (RULES) — READ THE FILE:** `verify_after_editing` 🔴 **after ANY consolidation/deletion/retraction GREP TWICE — for what should have SURVIVED, and for the OLD CLAIM. I skip this exactly when confident, which is exactly when it catches something (3 catches on 08-09, all operator-prompted). Write the RECOGNITION CUE — log string + visual symptom — not just the conclusion. Retract in place: `WITHDRAWN, DO NOT REINSTATE`** · `test_before_concluding` 🔴 **never publish a number I didn't measure; "validated" ≠ "works", RUN the real consumer; settle ambiguity by one-directional MECHANISM; label claims measured/source/recalled/assumed; whose decision was it, mine or the operator's; ⛔ A GREP THAT FINDS NOTHING PROVES THE PATTERN ABSENT, NOT THE EVENT** · `check_docs_before_measuring` 🔴 **grep the memory dir + `docs/` BEFORE deriving a dimension; the operator's ID of their own hardware beats my inference** · `eliminate_hypothesis_whole_family` 🔴 **dump EVERY candidate in ONE command before saying "not the cause"; name the CONSUMER** · `crash_recovery_checkpoint` · `use_dds_not_mavlink` · `camera_qgc_only` · `dkms_arch` · `wlan0_persistent_name`
**reference:** `px4_vio_collision` (🔴 **PX4 collision-prevention is MC-ONLY, dead on a rover; no PX4 path planner**) · `wfb_ng` · `wfb_rlyctl` · `wfb_cfg_apply` · `uart_map` · `services` · `known_fixes_archive` · `gcs_companion_interface` · `todos.md` · `ros2_nodes`/`ros2_topics` · `rover_odometry`
**project — ACTIVE:** `indoor_mapping_slam` (§13-16 = the 08-09 story) · `rover_autonav` (the wall-calibration record) · `perception_3d_costmap` · `autonomy_plan_reframe`
**project — other:** `l4_gemini_nav2_prereqs` · `vision_multicam_upgrade` · `l2_floortest_wheel0_reversed` · `wfb_undervoltage_dead_nic` (**DON'T raise the pot / usb_max_current_enable**) · `external_wifi_uplink` · `gcs_link_degraded` · `relay_ntp_setup` · `relay2_relaystn` (Pi4 brownout ⇒ powered hub) · `companion_network_degraded` · `boxb_pcie_usb` · `codexrelay_divergence` · `codexwork_token_in_remote` (🔴 **plaintext PAT**) · `codexwork_branches` (**auto-sync does NOT git-add NEW files**) · `rc_control_camera_retry_storm` (FIXED)

## [WORKING ON THIS MACHINE] — things the manuals do NOT cover
⚠️ **sudo NEEDS the password:** `printf '1987\n' | sudo -S …`
⚠️ **Boot clock is WRONG until NTP steps it** — don't correlate journals across a boot.
⚠️ **`pkill -f <pat>` MATCHES MY OWN SHELL** (the pattern is in my command line ⇒ self-kill, exit 144). Use `pgrep -x` / `pgrep -f "[p]attern"` then `kill <pid>`.
🔴🔴 **I AM A MAJOR CPU CONSUMER — BUDGET FOR IT.** `claude` runs **55-86% of a core** while I work. Its npm update check (84%) is **DISABLED** (`DISABLE_AUTOUPDATER=1` in `~/.claude/settings.json`); the pemmican MOTD hook (100%, ran on EVERY SSH login) is **DISABLED** (`chmod -x /etc/update-motd.d/90-pemmican`; power check = `vcgencmd get_throttled`, 0x0 clean; re-enable `chmod +x`).
🔑 **START A PERCEPTION MEASUREMENT, THEN GO QUIET.** Commanding throughout starved the camera on 08-09 and produced two wrong answers. Short spikes vanish before `ps` — use **`tools/cpu_catcher.sh`**.
⚠️ **No rate/CPU number is trustworthy without `ps -eo pid,pcpu --sort=-pcpu` first.**
⚠️ **Replays need `ROS_DOMAIN_ID=42`; the LIVE stack is domain 0.** ⚠️ **`fps` is INERT in QGC.**
✅ **I can read/write PX4 params myself:** `python3 ~/ros2_ws/tools/set_param.py NAME [value]` (refuses while armed). **Don't ask the operator to read QGC.**

## [CURRENT STATE — 2026-08-09 21:30. NEVER ARMED]
🗺️ **A GOOD ROOM MAP EXISTS: `~/house_map_v4.db`** (bag `~/map_run_20260809_185011`, 0 drops). Operator-verified: shape, objects, floor correct, **walls SINGLE**. `rtabmap_localization.yaml` points at it.
⏭⏭ **RESUME = RE-TEST LOCALIZATION FAIRLY.** The 08-09 test was **confounded by CPU**: jitter grew 5.7° (t=0-60 s) → 30.8° (t=210 s) as **colour collapsed 19.9 → 11.2 Hz mid-run**. Localization matches on COLOUR ⇒ I measured a starved camera, not the map. **Method: restart the camera, verify colour ~20-30 Hz, stop everything non-essential, measure 5+ min WHILE WATCHING THE COLOUR RATE**; only compare windows where colour held.
🔴 **I reported "2.4× better" from a 110 s window and PUSHED it — WRONG** (full 215 s gives 26.8°, worse than v2's 21°). Retracted in `972e345`. → [[feedback-test-before-concluding]]
🔑 **Duplicate walls were ODOM SCALE, not heading** — 19% × 22.4 m = 4.3 m of error in a 3.2×3.9 m room. `erpm_to_ms` → 0.003900. 🔑 **rescaling recorded TF ≡ driving with the fixed constant** (heading is gyro-derived) ⇒ same-bag re-map is the RIGOROUS test.
🔴 **The rover's own plate is in the map — 11.8% of the cloud** (ROI ignored: 234 rows ÷ decimation 4 ≠ integer). ✅ fixed for FUTURE maps (`Grid/DepthDecimation: "2"`). ⚠️ **v5 reprocessed gained ray-tracing SPIKES — NOT ADOPTED.** ⚠️ my claim it walls Nav2 in is **UNPROVEN/withdrawn**.
⏭ **Almirah still drawn several times** (x 0.4→1.6, y −0.85→−1.05): 14 cm thick vs 9 cm walls, darker+bluer ⇒ glossy-steel depth failure SUSPECTED not proven; **errs toward the rover ⇒ fails safe.** ⛔ don't "fix" by deleting cloud points.
⛔ **RECORD `/fmu/out/esc_status` IN FUTURE BAGS** — without it odometry can only be rescaled, not recomputed.
🔬 **DISPROVEN 08-09, don't re-propose:** map coverage · room light/auto-exposure (colour went UP in the dark — but tested in DAYLIGHT, so "a dark room hurts matching" is still open) · rover inside the 0.308 m depth min · camera-gyro rate-dependent scale (that was **my probe's arrival-time integration**) · `yaw_source: wheels` (skid-steer scrub ⇒ wheels cannot observe rotation; I proposed it twice).
🔴 **FC HEADING IS UNUSABLE — 2 UNTESTED SUSPECTS.** It is the **EKF's FUSED YAW, not the gyro** (3 IMUs, normal `CAL_GYRO*_ZOFF`). Suspects: the **single internal `bmm350`** (`EKF2_MAG_TYPE=1`, only yaw observer indoors) · **GPS** (`vehicle_gps_position` IS publishing 3.3 Hz — "pending hw" is STALE — `EKF2_GPS_CTRL=7`). Test `EKF2_MAG_TYPE=5` / `EKF2_GPS_CTRL=0` separately, several runs each. **Rover-only; the DRONE shares this FC and needs both.** → autonav_reference §6
⏭ **QUEUED:** bridge `vehicle_angular_velocity` to DDS (commented out at `dds_topics.yaml:60`; 3 fused IMUs @398 Hz, would **decouple odometry from the camera**) — needs a FC flash, so pair it with other FC work. Camera roll **1.54°** still uncorrected (§4).

## [TODOS] → memory/todos.md
**⏭ NEXT: re-test localization fairly (above) → then S1 kill test → T2 straight goal.** ✅ CLOSED: #20 yaw · #23 watchdog · #25 retry storm · #26 depth-glitch · **#26b (`RGBD/MaxOdomCacheSize: "0"`)** · #28 camera 30 fps. #21 gyro-yaw open.
1. Relay NTP · 2. 🔴 **NO onboard Wi-Fi fallback — recovery is WFB → relay:2222 ONLY** · 14. 🔶 **PAT: SSH done 08-09; STILL MUST REVOKE THE TOKEN + check `codex-relay`**
3+4+24. ❌ **DELETED, don't re-propose:** GS `rx_ring_size`, GS TX power (maxed), trim PX4 MAVLink rates.
5+7+8+9+10. Antenna tracker HW · /scan → Nav2 · #17 delete `camera_sw_node_obsolute.py` (+`rov_collision_stop`, `collision_manual_mode`) · Multicam Phase D (#25 **still keys `/dev/video0`** — close via Phase D)
27. ⏭ Reflex `/scan_3d` is a PARAM (`collision.scan_topic`), default still `/scan`. **Needs ONE low/overhanging object `/scan` misses before flipping.**
**[OUTDOOR] O1-O5:** STL-19 · DroneCAN GPS · lidar SLAM · GPS Nav2 · outdoor safety. **WFB parked: #22 = HARDWARE, reseat drone NIC-A ant0.**
⚠️ **`align_mode:=HW` untested** — would kill the depth-glitch class and save 71%/core. ⚠️ **Orbbec clone is GITIGNORED — a re-clone RESTORES the #26 bug** (patch: `codex-work 16665f5`).

## [FPV CAMERAS] — not in the manuals (autonomy camera is in setup_manual C2)
Both FPV-capable, port 6-2, **swap from QGC only**: **See3CAM_CU135** `usbcam-2560c1d1-241D8306-i00` 60 fps 720p MJPG · **LG Smart Cam** `usbcam-30c9009d-01.00.00-i00` 30 fps, cheaper CPU, **the current FPV cam**.
⛔ **Never key a camera by `/dev/videoN` or by-id** — only `usbcam-<vidpid>-<serial>-i<iface>`; Orbbec nodes appear/vanish with `rover-camera` and the Pi's `rpivid`/`pispbe-*` renumber them.
**⚡ FPV video costs `/scan` 28.4→22.3 Hz.** ⛔ **Do NOT modify the ffmpeg command line** (vetoed). `vision_streaming` STOPPED since 08-08 21:05.

## [WFB_NG] → reference_wfb_ng.md (**PARKED — only action left is HW: reseat drone NIC-A ant0**)
ch161 5GHz | drone-wfb@10.5.5.87 ↔ gs-wfb@10.5.5.77 | keys `/etc/{drone,gs}.key` | **Drone TX is flawless. When video breaks WFB's input queue is EMPTY — SUSPECT THE SOURCE.** Stats: TCP `127.0.0.1:8102` (GS 8103) JSON, **not** `wfb-cli`. **⚡ uplink loses 13.57% mavlink ⇒ NIC-A ant0 is 20 dB deaf (−48.5 vs −28.3).** `wifibroadcast@` restart's `rtw_mlmeext_disconnect` WARN+trace is **BENIGN**.
⚠️ **METHOD (cost a week):** compare payload `tx.incoming`→`rx.out` across 8102/8103. **Never `rx.all`** (double-counts 4 antennas). **Never infer radio health from MAVLink rates at `tcp:5760`.**

## [RELAY_STATION]
`vind-rly` | RPi5 | `ssh vind-admin@10.5.5.77` (**sudo NEEDS A PASSWORD ⇒ journalctl of other units = "No entries"**) | `~/codex-relay` | tunnel 2222→drone :22 | NO RTC | svcs: `wifibroadcast@gs`, `mavlink.router`, `ssh-tunnel-to-companion`, `relay_files_sync.timer` | wfb standalone (CURRENT) vs cluster (+CPE610, unconnected) | **Wi-Fi Direct P2P-GO `p2p-wlan0-0`, SSID `vind_rely`, ch149, 10.5.6.101/24 → QGC 10.5.6.50**

## [REPOS / GCS]
`codex-work`: `~/codex-work` → Companion_Computer_Pxlabs, branch **master** (origin/main stale) | `codex-relay`: on vind-rly → Relay_Station_Pxlabs | `ros2_ws`: `~/ros2_ws`, **main**, SSH remote ✅
GCS: G-Control.exe → pxlabs_cli.exe → SSH relay:2222 → companion:22 | QGC: github.com/ArvinVeiyon/PXLABS_qgroundcontrol @ PXLABS-integration
✅ **08-09: `codex-work` remote SWITCHED TO SSH** — no repo on this machine carries a token now (`codex-work` ✅ · `ros2_ws` ✅ · `PX4-Autopilot` public). 🔴 **BUT THE TOKEN IS STILL LIVE ON GITHUB — REVOKE IT** (github.com/settings/tokens). Switching the remote stopped it being USED; it did not rotate it. ⏭ **`~/codex-relay` on vind-rly was never checked for the same pattern.**

## [SERVICE / TROUBLESHOOTING QUICK INDEX] → full detail in setup_manual C, D2, E5
**AIDE timer DISABLED** (~3.5 h/day of a core). **`tfmini` DISABLED — ⚠️ MUST `systemctl enable --now tfmini` for the DRONE.** **`rover-ekf-bridge` DISABLED on purpose** (wheels-up limit-cycle hazard) — it already uses `LocalPositionMeasurementInterface`, velocity-only, so **the Q1 hook is half-built.**
no_MAVLink → `ttyAMA0` + the PX4 MAVLink instance | no_DDS → `microxrce-agent`, `ttyAMA4` | WFB_down → `wifibroadcast@drone`, `wlx*`, `/etc/drone.key` | **no_video → CPU-starvation latch FIRST (a restart does NOT clear it)** | no_scan/no_cloud → **half-dead camera restart first** | localization degrading → **measure the colour rate, then restart the camera**
